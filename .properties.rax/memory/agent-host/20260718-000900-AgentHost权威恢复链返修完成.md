# Agent Host 权威恢复链返修完成

- 时间：2026-07-18 00:09 CST
- 范围：`ExecutionRuntime/agent-host` H1-H2；H3 production adapter 仍为空。

## 已完成

- `Coordinator.InspectV1` 先验证 Journal；Host 仅在 Ready phase 返回 Ready，并做 fresh clock 与 exact ReadyRef 校验。
- Binding/Construction 新增 deterministic AttemptID、request digest 与 Journal write-ahead。
- 外部创建采用 canonical start-or-inspect；lost reply 后只 Inspect 原 attempt，无法恢复时持久化 Unknown。
- Construction progress CAS 丢回复通过 Journal exact Inspect 恢复，不重复调用 Factory start。
- Stop/Reconcile 遇到 planned/unknown attempt 必须保持 Indeterminate，不因已知 handle 清理完成而 Closed。
- 生命周期锁由全局锁改为 `HostID + StartID` 分片锁；不同 StartID 可并行。

## 验证口径

- ordinary 100 轮；
- race 20 轮；
- full ordinary/race/vet；
- import boundary 与 diff 检查。
