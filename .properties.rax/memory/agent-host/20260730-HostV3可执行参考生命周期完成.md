# HostV3 可执行参考生命周期完成

## 事件

在最新main上新增additive HostV3 reference coordinator。它复用atomic Claim/InputV3 sidecar与HostJournalV2，通过窄Owner pipeline消费Ready、Availability、CleanupClosure和CleanupResult exact refs。

## 验证事实

- 8个独立Host/State Plane SQLite handles发起32并发同Start；共享同进程reference pipeline mutex时只执行一次Owner pipeline；
- Start/Stop lost reply先重读Journal并Inspect原操作；
- 全部旧Host handles和旧pipeline关闭后，新pipeline从test-only SQLite projection store与新Journal handle恢复Inspect；
- V2/V3 Claim acceptance永久冲突；
- Claim/Closure splice、TTL expiry、Unknown均Fail Closed；
- Ready/Availability的ID、Revision、Epoch与expiry严格相等。

## 未完成

production Owner pipeline、Application/Runtime/Harness/6+1真实组合、Model Loop、CLI/daemon均未完成；本事件不改变production NO-GO。
production multi-process Owner intent/journal linearization同样未证明，不得从本黑盒推导跨进程exactly-once。
