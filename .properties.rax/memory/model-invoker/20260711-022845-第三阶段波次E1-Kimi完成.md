# 第三阶段波次 E1：Kimi开放平台完成

## 事件

- 时间：2026-07-11 02:28 CST
- 模块：`model-invoker`
- 阶段变化：E1第二家 Kimi开放平台按量 Route完成离线实现与验收
- 设计卡：`.properties.rax/design/model-invoker/provider-phase-3-wave-e1-kimi-platform.md`
- 计划依据：`.properties.rax/plan/model-invoker/phase-3-upstream-ecosystem.md`

## 当前官方合同刷新

2026-07-11刷新 Kimi API概述、模型列表、参数、reasoning、工具和错误文档后确认：

1. 中国开放平台按量 Endpoint为 `https://api.moonshot.cn/v1`，使用 `MOONSHOT_API_KEY` Bearer认证；
2. 当前主模型包括 `kimi-k2.7-code/highspeed`、K2.6、K2.5和 Moonshot V1文本系列；旧 K2预览、`kimi-latest`和 `kimi-thinking-preview`已下线；
3. K2.7始终开启 thinking且不能禁用，Preserved Thinking始终开启；K2.6可开关 thinking并可选保留；
4. `reasoning_content`是非流与流式推理载体，thinking工具循环需要完整回传历史推理；
5. Kimi Code会员使用 `api.kimi.com/coding`、`kimi-for-coding`和独立 Key，不得与开放平台按量 API互换。

## 落地产物

- 新增 `provider/kimi/`按量 Chat Adapter；
- 只绑定 `api.moonshot.cn/v1`、`MOONSHOT_API_KEY`与当前文本模型；
- K2.7不发送禁用开关或不存在的 effort档位，K2.6/K2.5把统一 reasoning意图转换为 `thinking.type`；
- 非流和流式 `reasoning_content`进入统一 reasoning output/delta；
- 当前无法忠实表示 preserved thinking历史时，thinking工具后续轮在 HTTP前拒绝；
- Partial Mode、官方工具、文件、Batch和多模态保持当前 slice外；
- 新增一条 callable Catalog Binding；与既有 Kimi Code两条非 callable控制记录并存但不互换；
- Catalog由50条变为51条：28条 callable、23条控制记录，对应九个 Runtime Adapter；
- Provider Matrix当前 Binding区块由 Catalog重新生成并通过漂移校验。

## 离线验收

以下命令在 `ExecutionRuntime/model-invoker/`实际通过：

- `go vet ./...`；
- `go test -count=1 ./...`；
- `go test -count=1 -shuffle=on ./...`；
- `go test -count=1 -race ./tests/kimi ./tests/deepseek ./tests/protocol/openaichat ./tests/catalog ./tests/catalogassets`；
- `go test -tags=integration -run '^$' ./tests/integration`，只编译；
- `GOMAXPROCS=8 go test -run '^$' -fuzz '^FuzzKimiSelectionNeverLeaksOrCallsUnknownModel$' -fuzztime=3s -parallel=8 ./tests/kimi`：通过25,231次；
- `go test -count=1 -covermode=atomic -coverpkg=./... -coverprofile=/tmp/model-invoker-kimi.cover ./...`：通过，合并语句覆盖率76.6%；
- `./scripts/verify-offline.sh`：退出0并输出 `model-invoker offline verification passed`。

## 边界与下一步

- 未读取真实 Key，未执行真实 API、真实套餐、认证成功或付费调用；
- Kimi按量状态仅为 `implemented_offline`，Kimi Code会员仍为 `planned + callable=false`；
- 下一步只启动 Z.AI按量 Route独立设计卡与离线实现；
- MiniMax、MiMo、Qwen、xAI、Meta、Sidecar、第三方名单和真实烟测仍按总计划保持后续顺序与授权边界。
