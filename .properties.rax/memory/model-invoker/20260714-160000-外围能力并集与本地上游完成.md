# 外围能力并集与本地上游完成快照

- 时间：2026-07-14 16:00:00 +08:00
- 模块：`ExecutionRuntime/model-invoker`
- 状态：实现、Review、离线黑白盒、集成标签测试与资产同步完成

## 完成内容

1. 新增Operation、Resource、Job、Realtime四类独立公共契约和统一生命周期；
2. 新增官方原生HTTP/WebSocket执行器、十家上游首批外围Spec、Gemini可信resumable upload；
3. 新增OpenAI-compatible、Ollama、llama.cpp和企业HTTPS自建调用身份与认证隔离；
4. 新增模型/能力白名单、资源归一化、Job状态、流式结果、错误/重试/脱敏边界；
5. 新增单元、负向、fuzz、HTTP/WebSocket黑盒、集成标签和真实Relay低成本探针。
6. 最终Review补齐Realtime模型单一所有者绑定：OpenAI/xAI query与Gemini `setup.model`均在拨号前核对。

## 最终验证

```text
git diff --check
go vet ./...
go test -count=1 ./...
go test -count=1 -shuffle=on ./...
go test -race -count=1 ./tests/operation ./tests/realtime ./tests/localcompat
go test -tags=integration -count=1 ./tests/integration
./scripts/verify-offline.sh
```

统一离线入口最终输出：`model-invoker offline verification passed`。

两项3秒Fuzz分别完成约82k与57k次执行，未出现panic或密钥泄漏。

## 真实边界

- Files List低成本探针结果：GPT Relay 503、Grok Relay 503、Claude Relay 401、Gemini Relay 404；均未证明外围Files API透传；
- 本机Ollama与llama.cpp端口未运行，真实本地推理为`not_run`；
- Codex订阅只证明官方Codex Harness可用，不等价于OpenAI Platform外围API；
- 未生成媒体、未上传文件、未创建Batch，没有可报告的生成token或可确认美元费用；
- 临时密钥未写入仓库、测试资产或配置文件。

## 后续入口

下一阶段只需在用户提供真实账号/本地服务后，按Provider和Operation逐项开启显式integration smoke；失败应修具体Spec或能力声明，不得fallback-open或推断兼容。
