# 第三方中转兼容与真实工具调用验证

- 时间：2026-07-14 00:48 CST
- 模块：`ExecutionRuntime/model-invoker`
- 基线：改动前完成统一离线验收并提交`469e69f`

新增独立`third-party-relay` Provider与显式可选Route Factory，复用Chat Completions、Responses、Messages、GenerateContent四个协议Driver。官方OpenAI/Anthropic/Gemini Provider的host/path门禁未放宽，Relay未加入默认Catalog和Builtin Registry。

离线文本、Tool Call、认证头、路径、精确模型、协议漂移、Endpoint、Factory opt-in测试通过。真实探针全部通过model-invoker的Relay Factory和Invoker执行，没有使用curl，密钥未写入仓库或测试资产。

真实结果：Gemini Chat、Grok Chat/Messages、GPT Chat/Responses、Claude Chat/Messages的文本与Tool Call通过；Gemini GenerateContent五次最小文本请求均收到HTTP 429“上游负载已饱和”，统一归一化为`rate_limit + retryable`，没有成功用量，Tool Call未继续消费。

已成功路线累计报告约5421 total tokens；中转站未返回美元账单，因此只保留Token与请求上限证据，不编造实际美元金额。不同路线的输入/推理Token差异明显，后续Profile必须把远端注入或不可见Harness作为Route级Delta处理。
