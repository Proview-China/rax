# 严格订阅调用面完成

## 事件

- 时间：2026-07-11 14:58 CST
- 范围：上游调用与统一封装最终候选D阶段
- 结果：官方明确允许第三方交互式编程客户端的订阅Route已完成严格离线实现与独立验收

## 已完成

1. Kimi Code、MiniMax Token Plan、MiMo Token Plan和Alibaba Coding/Token Plan共16条Route进入`implemented_offline + callable`，总Catalog为55条callable Binding、7条控制记录。
2. 新增4个独立Adapter身份和内建工厂：`kimi-code`、`minimax-token-plan`、`mimo-token-plan`、`alibaba-plan`；Gateway现有18个工厂均存在真实构造路径。
3. `plancompat`只批准文本、流、function tool与token usage；reasoning、结构化输出、并行工具、State、ProviderOptions、后台、批量与生产用途保持拒绝。
4. Runtime继续严格绑定计划专属Key前缀、官方host/path、Region、真实客户端User-Agent和个人/单租户/前台entitlement；不得伪造官方客户端或自动切换PAYG。
5. GLM Coding Plan仍为`official_client_only + research_only + callable=false`；xAI消费者产品仍无公开订阅API合同，保持不可调用。
6. 所有协议执行只使用fake Key与本机HTTP/SSE，没有读取真实Key、没有认证成功或套餐/付费调用。

## 验证

- `go test -count=1 ./tests/plancompat ./tests/routegateway ./tests/catalog ./tests/catalogassets ./tests/semanticmatrix`：通过；
- `go vet ./provider/plancompat ./routegateway ./tests/plancompat ./tests/routegateway`：通过；
- `go test -race -count=1 ./tests/plancompat ./tests/routegateway`：通过；
- `git diff --check`（排除用户并发白皮书、`.gitignore`和`tmp.document`）：通过。

## 边界

- `Usage`中的token计数不代表订阅余额或剩余额度；调用仍需新鲜`EntitlementState`。
- 订阅Route失败、限额或过期不会触发跨Region、跨Offering、跨Key或PAYG回退。
- 本阶段没有进入Profile、缓存策略、Context Engine、Runtime Kernel或工具执行器。

## 下一步

E阶段建立Route/Adapter/协议级Provider缓存事实资产，列出`CacheIntent`候选接缝与联合决策问题，但不实现任何缓存策略。
