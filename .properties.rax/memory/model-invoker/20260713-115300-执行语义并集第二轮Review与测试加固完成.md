# 执行语义并集第二轮 Review 与测试加固完成

- 时间：2026-07-13 11:53（Asia/Shanghai）
- 模块：`ExecutionRuntime/model-invoker`
- 状态：离线 Review 与测试加固完成；真实 API、OAuth、订阅账号和官方二进制 `not_run`
- 对应计划：`plan/model-invoker/execution-semantic-union-review-hardening-v2.md`

## 本轮结论

第二轮审查没有停在增加覆盖率，而是通过恶意输入、交叉身份、并发与真实生产 Adapter + fake process 集成反例，证明并修复了 P0/P1 合同缺口。

### 语义、Profile 与 Effect

- 封闭 `Context/Input/Content/Instruction` v1 tagged union，只允许命名空间化 `native_extension`；
- Profile 身份、fallback、权重/分数和零能力交集 fail closed；
- 文件 denied root、Move destination、symlink/type/hash/diff 边界收紧；
- 模型文字不能冒充 Tool execution evidence；
- Effect 与 Verification 建立双向关系，Attempt/Effect supersession 可追溯；
- Computer Use 缺少 after-state/external readback 时不得 verified；
- typed-nil structured repair 和无效 UTF-8 clone/digest 已修复并进入 fuzz regression corpus；
- metadata/profile constraints/nested extensions 拒绝高置信明文凭据。

### Runtime 与 Direct

- Plan-bound Ledger 拒绝跨 Intent/Plan/Attempt 绑定、未观察 Attempt 和指向未来/未知事件的 CausationID；
- Runtime 拒绝 Adapter 伪造显式 SessionID/TurnID；
- Effect/Verification 的 reciprocal refs 在终态决策前补齐，Projector 保留关联；
- Direct 流最终 Response 中首次出现的 Tool Call 会被物化；
- 非法/空 Tool Call、结果工具名篡改和非法 side-effect state fail closed；
- 并发重复 Tool Result 只续跑一次，等待者获得独立克隆；
- Close 发生在统一终态之前，source order 单调。

### Harness 与跨 Route 集成

- Claude/Qwen 纯结构化输出生成完整 MechanismAttempt；Attempt ID 改为 Execution scoped；
- live-smoke gate 增强高置信 `sk-*`、JWT、Basic/Bearer 和 AWS-like 明文拒绝，普通 token-budget 文字不误报；
- `tests/integration/harness_routes_offline_test.go` 直接使用 Codex、Claude、Gemini、Kimi、Qwen 生产 Adapter和受控 fake child process；
- 五路均走通 `Preflight → Manifest → Open → Runtime candidate → Reconcile → Verify → Result`；
- 同一 Adapter 双 Execution 并发时，Execution/Session/Attempt/Profile Manifest 保持隔离；
- 统一离线脚本清空 live switches、凭据、代理并使用新 HOME，完整 integration-tag 包中的真实 smoke 默认安全 Skip。

## 最终验证

- `bash ./scripts/verify-offline.sh`：退出码 0，末尾 `model-invoker offline verification passed`；
- 全仓 normal、`-shuffle=on -count=5`、race、vet、gofmt、tidy diff、module verify、catalog assets：通过；
- integration-tag 五路 Harness 全包 normal/shuffle/race：通过；
- 默认全仓 `-covermode=atomic -coverpkg=./...`：`76.6%`；
- 与 integration-tag profile 合并后：`76.7%`；
- Fuzz：Union Event `592,376`、Request Control `145,092`、Manifest Diff `306,053`、Event Replay `14,935`、Harness Frame `112,858`，合计 `1,171,314` 次，均通过；
- 三轮 benchmark：Profile compile `1.340-1.541 ms/op`；Manifest diff `102.9-247.3 us/op`；256-event replay `3.068-3.337 ms/op`；File snapshot `118.2-161.0 us/op`、`534.33-727.69 MB/s`。

## 保留的 P2 边界

1. Secret 扫描只覆盖高置信秘密形态，不对普通用户正文做高熵猜测，避免误杀合法内容；
2. File Observer 仍是通用 authorize-then-use 观察层；真正消除 TOCTOU 需要未来 Sandbox/fd/openat 执行层；
3. `ContentPart` v1 是闭集，新厂商内容必须升级版本化合同，不能借扩展字段伪装等价。

以上 P2 不阻塞当前离线合同，但必须在真实联调和后续 Sandbox/协议升级中保持可见。真实测试应逐 Route、最小 Token、先 Manifest 后 Prompt，并只走官方认证组件。
