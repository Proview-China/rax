# 宿主激活与十家上游再验证完成

## 事件

- 时间：2026-07-11 18:38 CST；
- 对象：`ExecutionRuntime/model-invoker`与对应`.properties.rax`资产；
- 结论：十家P0官方API再验证、公开原子`ActivationPlan`、`HostConfig/NewHost`、P0/P1 Gateway烟测入口和最终生命周期闭合均已完成离线实现、两棒审查与回归；
- 保持边界：没有读取、创建或复用真实Key，没有执行真实API、套餐、付费或公网Provider调用，没有进入Runtime Kernel、Profile、Context或缓存策略，没有提交或推送。

## 完成内容

1. 十家P0 Route逐家重新核对官方协议、Endpoint、认证、模型、流、工具、错误和商业边界；全部使用逐Route官方exact模型集合，未知、大小写漂移、别名与通配符在可信Resolver和Secret之前拒绝。
2. `catalog.ApplyActivationPlan`按精确RouteID、EvidenceDigest和AdapterID原子应用；任一项失败零部分生效，并返回稳定DecisionCode和canonical AuditDigest。
3. `routegateway.NewHost`把Catalog、激活计划、Binding/Secret/Subscription Resolver、Factory、Clock和HTTPClient收敛为一次构造事务；失败不返回部分Gateway，报告只含公开事实。
4. 十家P0 Gateway真实烟测入口已完成；P1只保留官方范围允许第三方交互式编码工具的Kimi Code与MiniMax Token自动入口。MiMo与Alibaba明确禁止脚本/API测试器，因此只保留离线Gateway验证，不能把测试器伪装成交互式客户端。
5. `Gateway.Close()`现在等待正在执行的首次Factory Build；若构造在关闭后返回，结果会安全关闭，晚到Closer错误进入生命周期汇聚并由Close以脱敏错误返回。
6. DeepSeek、Kimi、MiniMax、Z.AI公共构造器只允许官方远端Endpoint或loopback测试地址；Gateway统一核对流/非流实际响应模型，DeepSeek/Kimi另保留Provider层二次校验，服务端静默映射不能成为成功响应。

## 两棒审查修正

第二棒独立只读审查发现并推动修正两项P1：

1. 删除MiMo Token、Alibaba Coding与Alibaba Token Team的自动真实smoke及对应开关，消除条款冲突；
2. 将in-flight Factory Build纳入Pool lifecycle wait，并新增确定性交错测试：Build阻塞 → 并发Close必须等待 → Build返回后Closer只关闭一次 → Close错误可观测且不泄密。

修正后第二棒复核未发现新的P0/P1问题。

## 最终验收

- 统一离线入口通过：module verify、gofmt、tidy diff、diff check、vet、普通、shuffle、全仓race、integration仅编译、5项integration guard与Catalog资产门禁；
- Route Gateway定向普通与race测试通过，包含首次Factory Build与Close确定性交错；
- Catalog、Route Gateway、Qwen与Z.AI相关5项fuzz各3秒通过；
- `go test -count=1 -coverpkg=./... -coverprofile=... ./...`通过，全仓合并语句覆盖率78.0%；
- 机器资产继续为Catalog 62条、默认callable 39条、host-blocked订阅16条、研究/控制7条、18工厂、39×20语义矩阵780行、39条Provider缓存事实。

## 受保护对象

- 白皮书哈希：`3b70c876189332a19b383b780eb82882365446b3730786d855251aa7a5152fa3`；
- `.gitignore`哈希：`59b627880465606d96b2bfa781f2858cd46dc1b258d7114a8178259acdfc9353`；
- `tmp.document`内容聚合哈希：`08f89b811c31a9b58b2532e5568877d31a70df730f12b0e0b6c8f857ade54742`；
- 本轮未修改上述对象。

## 后续边界

- 真实Runtime宿主的BindingResolver、SecretResolver、SubscriptionAuthorizationResolver、Gateway原子替换与生命周期接线仍需相邻模块联合审核；
- 真实账号、Region容量、模型可用性、套餐额度、生产SLA和真实性能仍无结论，必须逐Route按条款单独授权；
- Anthropic Agent SDK、Gemini Interactions、xAI Hosted Tools及其他P2 native合同继续独立设计，不在本事件内扩张。
