# 第三阶段波次 E1：DeepSeek 完成

## 事件

- 时间：2026-07-11 02:12 CST
- 模块：`model-invoker`
- 阶段变化：波次 E由待启动进入实施，首家 DeepSeek完成离线实现与验收
- 设计卡：`.properties.rax/design/model-invoker/provider-phase-3-wave-e1-deepseek.md`
- 计划依据：`.properties.rax/plan/model-invoker/phase-3-upstream-ecosystem.md`

## 当前官方合同刷新

2026-07-11刷新 DeepSeek官方 Quick Start、Chat API Reference和 Anthropic兼容说明后确认：

1. 当前模型为 `deepseek-v4-flash`与 `deepseek-v4-pro`；
2. `deepseek-chat`与 `deepseek-reasoner`将在 2026-07-24 15:59 UTC废弃；
3. OpenAI兼容 Base URL为 `https://api.deepseek.com`，Anthropic兼容 Base URL为 `https://api.deepseek.com/anthropic`；
4. Chat提供 `thinking`、`reasoning_effort`与 `reasoning_content`；
5. Anthropic兼容端点会把 Claude别名和未知模型静默映射到 DeepSeek模型，因此 Praxis必须在 HTTP前做精确模型门禁。

## 落地产物

- 新增 `provider/deepseek/`独立 Runtime Adapter；
- 新增 `internal/compatprovider/`，只组合官方兼容协议 SDK transport、driver、Binding和脱敏，不拥有厂商身份或能力；
- `internal/protocol/openaichat/`新增可选方言请求、响应和流扩展缝隙；
- Chat主路径保留 `thinking`、reasoning effort转换、非流 reasoning output和流式 reasoning delta；
- Messages兼容路径只接受两个当前 v4精确模型，拒绝 Claude别名、未知模型、旧别名、被忽略的 reasoning budget和 tool error标记；
- 新增两条 callable Catalog Binding、设计卡、黑盒测试、fuzz和显式真实烟测入口；
- Catalog由48条变为50条：27条 callable、23条控制记录，对应八个 Runtime Adapter；
- Provider Matrix当前 Binding区块由 Catalog重新生成并通过漂移校验。

## 离线验收

以下命令在 `ExecutionRuntime/model-invoker/`实际通过：

- `go vet ./...`；
- `go test -count=1 ./...`；
- `go test -count=1 -shuffle=on ./...`；
- `go test -count=1 -race ./tests/deepseek ./tests/protocol/openaichat ./tests/catalog ./tests/catalogassets`；
- `go test -tags=integration -run '^$' ./tests/integration`，只编译；
- `GOMAXPROCS=8 go test -run '^$' -fuzz '^FuzzDeepSeekSelectionNeverLeaksOrCallsUnknownModel$' -fuzztime=3s -parallel=8 ./tests/deepseek`：通过27,064次；
- `go test -count=1 -covermode=atomic -coverpkg=./... -coverprofile=/tmp/model-invoker-deepseek.cover ./...`：通过，合并语句覆盖率76.7%；
- `./scripts/verify-offline.sh`：退出0并输出 `model-invoker offline verification passed`。

fuzz首次把单字符 Key与错误格式中的普通数字重合误判为泄密；测试改为对任意 fuzz输入派生固定高熵 Credential，再检查该完整 Credential是否穿透，保留最小化样本作为回归种子。没有发现实际密钥泄漏。

## 边界与下一步

- 未读取真实 Key，未执行真实 API、真实套餐、认证成功或付费调用；
- DeepSeek状态仅为 `implemented_offline`，不代表 `live_verified`或生产批准；
- 下一步只启动 Kimi开放平台按量 Route独立设计卡与离线实现；
- MiniMax、Z.AI、MiMo、Qwen、xAI、Meta、Sidecar、第三方名单和真实烟测仍按总计划保持后续顺序与授权边界。
