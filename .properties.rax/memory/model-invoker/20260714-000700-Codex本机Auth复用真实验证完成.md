# Codex本机Auth复用真实验证完成

- 时间：2026-07-14 00:07（Asia/Shanghai）
- 模块：`ExecutionRuntime/model-invoker`
- 状态：本机官方`auth.json`临时复用的CLI与App Server真实验证通过

## 做法与边界

读取当前`/home/proview/.codex/auth.json`的文件模式、大小和摘要，但不打印内容或摘要值。将其以0600复制到权限0700的一次性`CODEX_HOME`；不创建符号链接到源认证文件，防止官方组件刷新认证时改写用户源文件。

临时HOME的`codex login status`无需重新登录即报告ChatGPT登录。随后：

1. 官方`codex exec`通过官方ChatGPT HTTPS/SSE provider，以`gpt-5.6-sol`返回精确`praxis-local-auth-reuse-ok`，报告使用2281 tokens；
2. Praxis生产Codex App Server Adapter复用同一临时认证副本，完成最小无工具、只读、无副作用marker握手，integration单Route通过。

## 结论

本机官方认证文件可以作为用户明确授权的Harness私有HOME输入复用，无需模拟登录或由Praxis解析OAuth。正确产品边界是“选择/复制或挂载官方认证存储给官方Codex组件”，而不是把token读入Praxis配置、Provider请求或审计事件。

收口时必须比较源文件摘要未变化，删除整个临时HOME，并再次确认常驻Codex登录状态正常。
