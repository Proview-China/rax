# 第三阶段波次 E1：Z.AI按量完成

## 事件

- 时间：2026-07-11 02:42 CST
- 模块：`model-invoker`
- 阶段变化：E1第三家 Z.AI按量 Route完成离线实现与验收
- 设计卡：`.properties.rax/design/model-invoker/provider-phase-3-wave-e1-zai.md`

## 官方合同与产物

- 标准按量 Endpoint固定为 `https://api.z.ai/api/paas/v4`，使用 `ZAI_API_KEY`；GLM Coding Plan的中国专属 Endpoint、Key和额度保持独立 Offering；
- 当前文本模型以官方 Chat枚举精确校验，视觉/Agent/Hosted Tool不进入当前 slice；
- GLM-5.2 effort、其他 thinking模型开关、`reasoning_content`和 body/stream `request_id`完成映射；
- Chat协议增加可选 Provider finish reason和流 Envelope元数据缝隙；`sensitive`、`model_context_window_exceeded`和 `network_error`分别归一化；
- 1000-1321业务错误按认证、余额、模型、策略、限流、服务和订阅边界分类，订阅错误不触发 PAYG或 Key回退；
- preserved thinking当前关闭，无法保真回传历史推理的 thinking工具后续轮在 HTTP前拒绝；
- 新增 `provider/zai/`、`tests/zai/`、integration入口和一条 callable Catalog Binding；
- Catalog由51条变为52条：29条 callable、23条控制记录，对应十个 Runtime Adapter。

## 离线验收

- `go vet ./...`、全量普通、shuffle与定向 race通过；
- `FuzzZAISelectionNeverLeaksOrCallsUnknownModel`运行3秒通过19,575次；
- `go test -count=1 -covermode=atomic -coverpkg=./... -coverprofile=/tmp/model-invoker-zai.cover ./...`通过，合并语句覆盖率76.4%；
- `go test -tags=integration -run '^$' ./tests/integration`只编译通过；
- `./scripts/verify-offline.sh`退出0并输出 `model-invoker offline verification passed`。

## 边界与下一步

- 未读取真实 Key，未执行真实 API、真实套餐、认证成功或付费调用；
- Z.AI状态仅为 `implemented_offline`，GLM Coding Plan继续 `planned + callable=false`；
- 下一步只启动 MiniMax按量 Route独立设计卡与离线实现。
