# 上游调用最终候选A-F完成

## 事件

- 时间：2026-07-11 15:11 CST
- 范围：`model-invoker`上游调用与统一封装最终候选A→F
- 结果：资产纠正、Route Gateway、统一语义矩阵、严格订阅调用面、Provider缓存事实和最终离线验收全部完成并同步

## 最终产物

1. 根包`RouteInvoker`固定为Policy/Authorization/Audit层；`routegateway.Gateway`固定为Runtime Binding、Secret、18工厂、Adapter池/Lease和实际调用组合层。
2. Catalog保持62条记录：55条`implemented_offline + callable`、7条控制记录；55条均有真实内建工厂构造与Route级Capabilities合同。
3. 统一语义机器矩阵为55 Route × 20能力 = 1100行，覆盖6协议和18 Adapter；公共结构保持加法式候选，不提前冻结相邻消费者合同。
4. Kimi Code、MiniMax/MiMo Token Plan、Alibaba Coding/Token Plan共16条在严格个人交互门禁下离线可调用；GLM Coding Plan和xAI消费者路线继续不可调用。
5. Provider缓存机器事实为55行；只有xAI Responses暴露严格`prompt_cache_key`传输，model-invoker不拥有key生成、TTL、存储、命中、淘汰或Route优化策略。
6. 最终审核清单按“已完成/候选待联合审核/明确延期/需用户决定/未运行真实验证”五类保存。

## 最终验证

- `gofmt`：无未格式化文件；
- `go mod tidy -diff`：无差异；`go.mod`和`go.sum`哈希前后不变；
- `scripts/verify-offline.sh`：通过，包含module download/verify、diff check、vet、普通、shuffle、全仓race、integration仅编译和资产门禁；
- 全部29个现有fuzz入口逐项运行1秒：全部通过；
- `go test -count=1 -covermode=atomic -coverpkg=./... -coverprofile=... ./tests/...`：通过，合并语句覆盖率77.8%；
- 白皮书、`.gitignore`和`tmp.document`测试前后内容哈希不变；没有提交或推送。

## 候选与延期

- 待联合审核：语义矩阵v1候选、Route Gateway宿主接线、`CacheIntent`所有权与隔离合同；
- 明确延期：Profile、Context Engine、Runtime Kernel、缓存策略、工具执行、多模态、Hosted/Batch/Realtime/后台、Sidecar与第三方托管；
- 需用户决定：Meta精确Route、第三方首批名单、相邻模块联合审核和逐Route真实烟测授权；
- 未运行：真实Key/登录态、认证成功调用、套餐/付费调用、真实配额、生产容量、性能与SLA验证。

## 结论

本候选只声明离线协议、安全、授权、生命周期和机器资产闭环，不声明`live_verified`或生产可用。后续工作必须从最终审核清单重新立项并取得对应授权。
