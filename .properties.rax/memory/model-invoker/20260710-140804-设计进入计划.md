# 模型调用器：设计进入第一阶段计划

- 时间：2026-07-10 14:08:04 CST
- 模块：`model-invoker`
- 阶段变化：设计 → 第一阶段计划与实现

## 事件

用户明确要求把现有 `model-invoker` 设计转换为实际可实施的计划，并在 `ExecutionRuntime/model-invoker/` 编写代码。第一阶段只覆盖 Go 统一调用内核和 OpenAI Provider，不引入其他 Provider 空壳或 TypeScript Sidecar。

## 已确定决定

1. Go module 位于 `ExecutionRuntime/model-invoker/`；
2. OpenAI 是第一个 Provider，使用官方 `openai-go/v3 v3.41.1`；
3. 同时实现 Responses 与 Chat Completions，在 Provider 内分别解析后转换为 Praxis 统一语义；
4. 第一阶段聚焦 Agent 所需的文本、工具、结构化输出、推理配置、状态、流、用量、错误、取消和超时；
5. TypeScript Sidecar 和 IPC 协议延期到确有 Provider 需要时再设计；
6. 本轮不创建、不读取、不复用真实 API Key，只使用注入客户端、fake 和 `httptest` 完成离线验证；
7. 真实 API 烟雾测试等待用户之后提供 API 与测试边界。

## 当前状态

详细计划已写入 `.properties.rax/plan/model-invoker/README.md`，实现进入执行中。根目录 `README.md` 的既有删除不属于本模块，本次保持不动。
