# 第三阶段波次 E1：MiniMax按量完成

## 1. 事件

- 时间：2026-07-11 03:08:48 CST
- 模块：`model-invoker`
- 波次：第三阶段 E1 MiniMax按量
- 设计卡：`.properties.rax/design/model-invoker/provider-phase-3-wave-e1-minimax-payg.md`
- 计划依据：`.properties.rax/plan/model-invoker/phase-3-upstream-ecosystem.md`
- 结果：三协议离线实现、测试、Catalog和说明资产完成；真实调用继续延期

## 2. 落地产物

1. 新增 `provider/minimax/`独立 Runtime Adapter，默认协议为 Anthropic Messages，同时实现 OpenAI Chat Completions与 Responses；
2. 精确允许 `MiniMax-M3`及当前 M2.7/M2.5/M2.1/M2文本模型，响应模型与请求模型不一致时 fail closed；
3. Messages保留 thinking/signature与工具 continuation；MiniMax服务端 `tool_use`缺少 Anthropic新 `caller`字段时，只在服务端输出进入内部 State前规范化为 direct，发回 MiniMax时移除该内部字段；
4. Chat固定 `reasoning_split=true`，按 M3/M2.x合同处理 thinking，并把官方累积 reasoning/text流转换为不重复的 portable delta；
5. Responses保留 typed message/function/reasoning输出，清除官方 `store=false`路线不具备的服务器 continuation State并拒绝输入 State；
6. `sk-cp-*` Token Plan Key在按量 Adapter配置阶段拒绝；原 `minimax.token-plan`两条控制记录继续使用独立秘密引用、`interactive_coding_only`、无 Adapter且 `callable=false`；
7. Catalog由52条扩展为55条：32条 callable Binding、23条控制记录；新增三条 MiniMax按量 Binding均为 `implemented_offline`；
8. 新增 `tests/minimax/`黑盒与 fuzz、`tests/integration/minimax_smoke_test.go`三重显式烟测入口；统一离线脚本新增 MiniMax按量/Token Plan凭据和烟测开关清理。

## 3. 测试证据

以下命令均在 `ExecutionRuntime/model-invoker/`实际执行并退出0：

- `go test -count=1 ./...`：全量普通测试通过；
- `go test -shuffle=on -count=1 ./...`：全量随机顺序测试通过；
- `go test -race -count=1 ./tests/minimax ./tests/protocol/openaichat ./tests/protocol/openairesponses ./tests/protocol/anthropicmessages`：MiniMax与三个复用协议定向 race通过；
- `go test -run='^$' -fuzz='^FuzzMiniMaxSelectionNeverLeaksOrCallsUnknownModel$' -fuzztime=3s ./tests/minimax`：通过26,436次执行；
- `go test -count=1 -covermode=atomic -coverpkg=./... -coverprofile=/tmp/model-invoker-minimax.cover ./...`：通过，合并语句覆盖率76.2%；
- `bash ./scripts/verify-offline.sh`：退出0并输出 `model-invoker offline verification passed`；实际覆盖依赖校验、gofmt、tidy diff、`git diff --check`、vet、普通、shuffle、全量 race、integration仅编译和 Catalog资产门禁。

黑盒测试覆盖 Messages两轮 thinking/tool往返、Chat非流与累积 SSE去重、Responses typed输出与无服务器 State、M3/M2.x thinking差异、Token Plan Key、未知/模态模型、错协议 Endpoint、1008 Billing错误、Credential格式脱敏和静默模型映射拒绝。

## 4. 安全与延期边界

- 未读取 `MINIMAX_API_KEY`、`MINIMAX_TOKEN_PLAN_API_KEY`或任何真实 Credential；
- 未执行真实认证、真实模型、真实套餐或付费调用；
- integration入口只编译，未选择 `TestMiniMaxLiveSmoke`；
- `implemented_offline`不代表 `live_verified`或 `production_approved`；
- 多模态、priority service tier、Prompt Cache、原生旧文本接口与 Token Plan执行不在本切片；
- 下一步只启动 MiMo按量设计卡与离线切片；Sidecar、第三方托管首批名单和真实烟测继续延期。
