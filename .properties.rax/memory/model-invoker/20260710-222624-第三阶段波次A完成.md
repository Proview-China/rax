# 第三阶段波次 A 完成

## 事件

- 模块：`model-invoker`
- 时间：2026-07-10 22:26:24 CST
- 状态：波次 A 已完成离线实现与验收；波次 B0 待启动
- 实现位置：`ExecutionRuntime/model-invoker/`
- 计划依据：`.properties.rax/plan/model-invoker/phase-3-upstream-ecosystem.md`

## 本次落地产物

1. `upstream/` 固定 Model Family、Provider、Offering、Deployment、Protocol、Endpoint、Credential七维 Route身份，并实现确定性身份/摘要、Endpoint安全解析、Credential引用与 audience/路线约束、`allowed_usage`策略及 SDK无关 MappingReport。
2. `catalog/` 落地机器可读 Catalog、严格编解码、语义/重复/状态迁移校验、官方来源、TTL、失效、证据摘要、能力/错误/流方言、实现与资产证据。
3. `catalog/schema/catalog-v1.schema.json` 与嵌入的 `praxis.upstream-catalog/v1` Schema保持字节一致，并由测试核对完整 Go文档形状。
4. Provider Matrix的“当前 Binding”Markdown区块由 Catalog生成并接受漂移校验；矩阵其他研究路线不会因此成为可调用 Provider。
5. 统一离线入口为 `ExecutionRuntime/model-invoker/scripts/verify-offline.sh`；`.github/workflows/model-invoker.yml`调用同一脚本。脚本屏蔽真实凭据与烟测开关，integration只编译，不选择真实烟测。

## 当前 Binding与 AdapterID

| Route ID | Catalog Provider | Runtime AdapterID | 状态 |
|---|---|---|---|
| `openai.direct.payg.responses` | `openai` | `openai` | `implemented_offline` |
| `openai.direct.payg.chat_completions` | `openai` | `openai` | `implemented_offline` |
| `anthropic.direct.payg.messages` | `anthropic` | `anthropic` | `implemented_offline` |
| `google.gemini-developer.payg.generate_content` | `google.gemini-developer` | `gemini` | `implemented_offline` |

AdapterID由 `tests/catalogassets`直接对照现有 Provider公开 `ProviderID`；Catalog Provider身份与 Runtime Registry ID保持显式映射，不互相冒充。

## 实际验收

- `bash ExecutionRuntime/model-invoker/scripts/verify-offline.sh`：退出 0。实际覆盖 `go mod download`、`go mod verify`、gofmt、`go mod tidy -diff`、`git diff --check`、`go vet`、全量测试、shuffle、race、integration仅编译和 `catalogassets`校验。
- `go test -count=1 ./tests/upstream ./tests/catalog ./tests/catalogassets`：通过。
- `FuzzEndpointResolutionNeverReturnsUnsafeURL`：3秒，通过，349,949次执行。
- `FuzzCatalogDecodeValidateAndDigest`：3秒，通过，18,291次执行。
- `FuzzCatalogArtifactPaths`：3秒，通过，13,548次执行。

## 边界

- 没有新增 Provider；Catalog只登记现有三家 Provider的四条精确协议 Binding。
- 没有使用真实 API Key、消费者登录态或真实套餐，没有执行认证成功的模型调用。
- `implemented_offline`只表示离线实现与验收完成，不表示真实模型可用、生产支持、SLA、容量或成本结论。
- GitHub Actions工作流已接入统一入口；本次记录的是本地脚本实际通过，不冒充远端 CI运行结果。

## 下一步

启动波次 B0：建立 `internal/protocol` 的 Binding、Driver、Failure和 Provider身份注入反例。B1及后续协议抽取、其他 Provider、订阅、云托管、Sidecar与真实烟测均不在本事件完成范围内。
