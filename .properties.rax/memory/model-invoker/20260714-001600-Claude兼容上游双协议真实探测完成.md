# Claude兼容上游双协议真实探测完成

- 时间：2026-07-14 00:16（Asia/Shanghai）
- 模块：`ExecutionRuntime/model-invoker`
- 状态：用户授权第三方HTTPS上游基础非流文本协议探测通过；生产Route尚未建立
- 上游：`https://viewpro.top`
- 模型：`claude-opus-4-8`
- 凭据：仅进入临时请求进程，不记录值

## `/v1/messages`

- HTTP 200；
- 响应`type=message`，模型未漂移；
- `stop_reason=end_turn`；
- 精确返回`PRAXIS_MESSAGES_OK`；
- usage：18 input、5 output、cache 0、server tool use 0。

## `/v1/chat/completions`

- 使用标准`Authorization: Bearer`；
- HTTP 200；
- `id`存在，`object=chat.completion`，模型未漂移；
- `finish_reason=stop`；
- 精确返回`PRAXIS_CHAT_OK`；
- 标准usage为18 prompt、5 completion、23 total；
- 附加兼容字段`output_tokens=0`与标准completion计数不一致。

## 结论与边界

两条端点的基础非流文本生成兼容已验证，总量为46个输入/输出token量级，远低于用户5美元硬预算。没有继续测试stream、Tool Call、结构化输出、图片、缓存或错误协议，因此不得标记为完整Anthropic/OpenAI兼容。

Praxis当前官方Anthropic和OpenAI Adapter只信任官方host或loopback，这是刻意的防重定向边界；不能为了接入该域名而放宽官方Adapter。若要生产接入，应建立独立Route/Factory，显式声明第三方owner、双协议能力、usage residual、模型精确集合、endpoint pin与凭据引用，再逐项做conformance和真实验证。
