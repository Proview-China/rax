# Provider缓存事实交接完成

## 事件

- 时间：2026-07-11 15:02 CST
- 范围：上游调用与统一封装最终候选E阶段
- 结果：Provider缓存传输事实已形成Route级机器资产和独立漂移门禁；缓存策略仍未进入model-invoker

## 已完成

1. 新增`cachefacts`和`cmd/cachefactsgen`，从活动Catalog确定性生成55条callable Route、18个Adapter、6协议的缓存传输事实。
2. 机器CSV逐Route固定Catalog声明支持、Request控制、key所有权、TTL、State关系、read/write usage、TotalTokens规则、错误合同、限制及Evidence状态/TTL/失效时间/digest。
3. OpenAI Responses/Chat、Anthropic Messages、Gemini GenerateContent、Bedrock Converse和Bedrock InvokeModel分别绑定实际归一化代码入口；事实只声明“Provider报告时归一化”，不推断服务端一定缓存。
4. 只有xAI Responses暴露严格Provider命名空间的`prompt_cache_key`传输；其余54条Route没有统一缓存控制面。
5. `CacheIntent`只记录为上层调用意图→Route绑定→Provider严格选项的候选接缝；key作用域、TTL、隔离、失效、计费、State分离和Route选择必须经过跨模块联合审核。
6. 没有实现key生成、存储、TTL、预热、写入策略、旁路、淘汰、跨Route复用或成本优化。

## 验证

- `go test -count=1 ./tests/cachefacts ./tests/openai ./tests/anthropic ./tests/gemini ./tests/bedrockruntime ./tests/bedrockmantle ./tests/xai ./tests/zai`：通过；
- `go vet ./cachefacts ./cmd/cachefactsgen ./tests/cachefacts`：通过；
- `go test -race -count=1 ./tests/cachefacts`：通过；
- 生成器输出与`.properties.rax/design/model-invoker/provider-cache-facts-v1candidate.csv`逐字节一致；
- CSV共56行：1行表头和55条Route事实；
- `git diff --check`（排除用户并发对象）：通过。

## 边界

- `CacheReadTokens`/`CacheWriteTokens`是Provider传输明细，不是命中、价格、余额或策略证明。
- Prompt Cache、`State`、Provider continuation、Raw响应和AWS SDK credential cache保持不同类型和生命周期。
- 没有读取真实Key、没有Provider公网或付费调用。

## 下一步

F阶段运行全量离线验收、生成资产漂移检查、全仓race/integration仅编译/fuzz/覆盖率，并同步最终design/plan/module/properties状态。
