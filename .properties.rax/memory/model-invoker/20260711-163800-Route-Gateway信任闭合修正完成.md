# Route Gateway信任闭合修正完成

## 事件

- 时间：2026-07-11 16:38 CST；
- 对象：`ExecutionRuntime/model-invoker`与对应`.properties.rax`资产；
- 结论：独立审核确认的5项最终候选语义缺口已全部修正；默认Catalog、机器资产、代码、测试和说明已恢复一致；
- 保持边界：未进入Runtime Kernel、Model Profile、Context Engine或缓存策略；未读取真实Key、执行真实套餐/付费/公网Provider调用；未提交或推送。

## 完成内容

1. 可信订阅身份/权益：默认16条已实现订阅Route降为`callable=false + blocked_by_host_trust`；只有宿主注入`SubscriptionAuthorizationResolver`并采用经审核激活的Catalog后才可调用。调用方自带build-manifest、runtime-observed或active entitlement claim全部在Resolver、Binding、Secret、Factory和Provider前拒绝。
2. Secret前模型门禁：受限订阅Route全部使用逐Route `static_catalog + exact_provider_id`；模型字符级校验位于可信Resolver、Binding和Secret之前。Alibaba Coding Plan与Token Plan Team使用两个独立官方exact-string集合。
3. Alibaba官方白名单：Coding Plan固定10个精确模型ID并明确拒绝`GLM-5.1`/`glm-5.1`等未列值；Token Plan Team只纳入官方文本模型，不与Coding Plan或图像模型形成隐式并集。
4. Close可观测：轮换空闲Adapter Close错误进入安全生命周期汇聚；并发`Gateway.Close`等待已登记轮换Close并返回安全聚合；stale Lease最后释放继续由当前Resolve/Invoke/Stream Close返回；原始错误只留在unwrap链。
5. Factory入池门禁：Provider身份、生命周期Closer、Endpoint非空以及scheme/host/base-path/编码安全全部在发布前验证；失败结果安全Close且不污染池。关闭期间的轮换不会继续构造或发布孤儿Adapter。

## 当前机器事实

- Catalog总数：62；
- 默认callable：39，覆盖14个默认活跃Runtime Adapter；
- host-blocked订阅：16，保留4个订阅工厂与离线实现；
- 其他研究/控制记录：7；
- 语义矩阵：39 Route × 20能力 = 780行，6协议；
- Provider缓存事实：39行；
- 内建Factory Registry：18个。

## 实际验收

- 定向测试：`go test ./tests/routefacade ./tests/routegateway ./tests/catalog ./tests/catalogassets ./tests/plancompat`通过；
- 定向race：`go test -race ./tests/routegateway`通过；
- 统一入口：`./scripts/verify-offline.sh`通过，包含module verify、gofmt、tidy diff、diff check、vet、普通、shuffle、全仓race、integration仅编译和资产门禁；
- 相关fuzz各3秒通过：SecretMaterial格式化12,498次、Catalog decode/validate/digest 21,444次、Catalog资产路径1,956次；
- 全仓覆盖：`go test -count=1 -coverpkg=./... -coverprofile=... ./...`通过，合并语句覆盖率77.5%；
- 机器资产复核：语义CSV 781行含header，39 Route/14 Adapter/6协议/780数据行；缓存CSV 40行含header，39数据行。

## 受保护对象

- 白皮书哈希：`3b70c876189332a19b383b780eb82882365446b3730786d855251aa7a5152fa3`；
- `.gitignore`哈希：`59b627880465606d96b2bfa781f2858cd46dc1b258d7114a8178259acdfc9353`；
- `tmp.document`内容聚合哈希：`08f89b811c31a9b58b2532e5568877d31a70df730f12b0e0b6c8f857ade54742`；
- 本次实现未写入上述对象；它们保留为用户既有工作区内容。

## 后续边界

- 真实宿主Resolver实现和激活Catalog属于Runtime相邻模块联合审核，不在本事件内；
- Profile、Context、CacheIntent/缓存策略与Runtime Kernel继续延期；
- 真实账号、Region容量、模型可用性、套餐额度和生产SLA仍无结论，必须逐Route单独授权验证。
