# 第三阶段波次 B5/B6 完成

## 事件

- 时间：2026-07-11 00:43 CST
- 模块：`model-invoker`
- 状态：波次 A与 B已完成离线实现与验收；波次 C待启动
- 计划依据：`.properties.rax/plan/model-invoker/phase-3-upstream-ecosystem.md`

## 本次落地产物

1. `internal/protocol/failure_extract.go`成为四协议共享的安全首轮提取边界，统一 context、既有公共错误、重定向、transport、malformed payload与单行限长文本处理。
2. Gemini GenerateContent删除最后一套通用错误重复分支，改用共享 `BeginFailureExtraction`、`ExtractCommonFailure`与 `BoundedFailureText`；SDK特有 APIError字段仍由具体协议包忠实提取。
3. 新增 `internal/adaptercore/context_error.go`，Capabilities只允许 `context.Canceled`和`context.DeadlineExceeded`进入公共 unwrap链，未知原生错误不保留。
4. OpenAI、Anthropic与 Gemini Provider的 `errors.go`删除旧 `normalizeError`和 SDK Raw提取，只保留配置/验证错误构造与 Provider方言分类。
5. 新增共享 Failure黑白盒测试、context sentinel反例、Provider旧提取器 AST反例，以及公共导出签名不含三家 SDK类型的 AST门禁。

## 实际验证

- 修改前基线 `go test -count=1 ./...`：通过。
- B5/B6定向测试：`go test -count=1 ./tests/core ./tests/protocol ./tests/openai ./tests/anthropic ./tests/gemini ./tests/protocol/...`：通过。
- 统一入口 `./scripts/verify-offline.sh`：退出0，覆盖依赖、格式、tidy、diff、vet、普通、shuffle、race、integration仅编译与 Catalog资产。
- 20项现有 fuzz全部实际通过。`FuzzCatalogArtifactPaths`在连续16 workers的3秒批次曾于停止边界返回一次 `context deadline exceeded`；单独5秒复验通过34,593次，后续目标限制8 workers后均稳定通过。
- `go test -count=1 -coverpkg=./... -coverprofile=/tmp/model-invoker-b5b6.cover ./...`：通过；`go tool cover -func`合并语句覆盖率为81.0%。

## 安全与范围结论

- SDK错误、SDK类型、`http.Request`、Credential和未知原生 cause仍不能穿透公共边界；只有标准 context取消/截止 sentinel可 unwrap。
- Provider方言继续拥有能力、分类和扩展语义；协议 driver没有硬编码原厂 Provider身份。
- 本次没有新增 Provider，没有读取真实凭据，没有执行真实 API、真实订阅套餐、付费调用或认证成功烟测。
- 81.0%覆盖率只记录现状，尚未成为百分比硬门禁。

## 下一步

启动波次 C。编码前先用当前官方来源核验 Kimi Code、MiniMax Token Plan、GLM、MiMo、Alibaba与 xAI消费者计划边界；可执行路线只落地明确允许的个人交互式范围，`terms_blocked`或证据不足路线只建 Catalog与拒绝行为。
