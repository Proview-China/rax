# 模型调用器：第三阶段离线实施启动

- 时间：2026-07-10 21:43:54 CST
- 事件性质：阶段授权与实施启动
- 阶段变化：第三阶段由“计划待审核”进入“离线实施”
- 当前波次：A — UpstreamRoute、Offering、Credential、Catalog与证据门禁

## 用户授权

用户明确要求根据检查结果更新 design与 plan，并按照计划逐一完成。随后进一步明确：

- 当前阶段不使用真实 API；
- 当前阶段不使用真实订阅套餐；
- 先完成全部离线构建与测试；
- 所有真实 API与真实套餐测试统一留到最后。

## 实施边界

- 按第三阶段计划 A→H 顺序推进；
- 允许创建和修改本阶段设计已经覆盖的 Route、Catalog、协议驱动、Provider、Sidecar和测试；
- 普通测试必须清除真实 Key并禁止外网依赖；
- `terms_blocked` 路线只实现 Catalog、策略和拒绝测试，不实现规避条款的调用；
- `unverified` 路线在证据补齐前不得声明可调用；
- 不读取消费者登录态，不创建或复用真实 Key，不执行付费调用；
- 真实烟测入口可以编译，但不得执行认证成功请求。

## 当前已确认基线

- OpenAI Responses/Chat Completions、Anthropic Messages、Gemini GenerateContent已完成离线实现；
- 当前全模块普通测试、race、vet与 integration仅编译检查通过；
- 当前合并覆盖率为 80.9%；
- 其余套餐、云托管、重点厂商和第三方路线尚未进入代码。

## 下一步

先完成波次 A并独立验收，再进入波次 B的通用协议驱动抽取。每个波次完成后同步计划清单、模块说明、项目索引与新的粗粒度 memory。
