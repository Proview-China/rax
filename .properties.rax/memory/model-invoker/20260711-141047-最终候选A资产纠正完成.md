# 最终候选 A：资产纠正完成

## 事件

- 时间：2026-07-11 14:10 CST
- 范围：上游调用与统一封装最终候选 A阶段
- 结果：上一轮完成声明、RouteInvoker职责、xAI手工矩阵、覆盖率与tmp.document描述已纠正并通过资产门禁

## 纠正事实

1. 根包 `RouteInvoker`准确定位为 Route Policy/Authorization/Audit层；它依赖预构造Registry/Invoker，不解析Secret、不构造Adapter、不管理实例生命周期。
2. 增加`RoutePolicyInvoker`/`NewRoutePolicyInvoker`职责准确别名；旧名称和版本常量作为候选迁移兼容别名保留。
3. 统一语义改为`praxis.model-invoker.semantic/v1candidate`，Route Policy改为`praxis.model-invoker.route-policy/v1candidate`；均不得称最终冻结。
4. Provider手工矩阵将`xai.direct + research_only`纠正为`xai.api + implemented_offline` Responses路线；xAI gRPC继续明确延期。
5. module README将第三阶段最终历史覆盖率纠正为76.7%，同时保留Qwen波次当时76.5%的真实历史。
6. 上一阶段快照已明确：`tmp.document/`现场仍存在且归属不确定；本任务不删除或修改。
7. 新增最终候选设计与A→F活动计划；旧计划不删除，继续作为阶段性陈旧计划。

## 验证

- `go test -count=1 ./tests/routefacade ./tests/catalog ./tests/catalogassets`：通过；
- `git diff --check`：通过；
- 新增资产门禁锁定xAI手工矩阵、76.7%历史记录、tmp.document真实描述和candidate状态。

## 工作区保护

未修改用户并发的`Praxis白皮书/Praxis白皮书总纲.md`、根级`.gitignore`与`tmp.document/`。当前`tmp.document/上下文-运行时.md`存在，归属不确定。

## 下一步

B阶段在同一Go module新增`routegateway/`组合包，补齐SecretResolver、RuntimeBindingResolver、14 Adapter工厂、实例生命周期、Route级Capabilities与39条callable Route真实离线构造路径。
