# MCP真实stdio与Streamable HTTP受治理Call软件门YES

时间：2026-07-18 12:43:00 +08:00

## 事件

- official MCP Go SDK `tools/call`第一切片不再只有in-memory协议证据；
- 同一Tool-owned `MCPExecutionCommand`、Runtime public V3 actual-point authorization、exact
  Command/Association/Session current复读与create-once admission链，已分别穿过真实子进程
  stdio与loopback Streamable HTTP Server fixture；
- Provider回包仍只生成immutable `MCPProtocolReceiptV1`，不直接升级为ToolResult、Runtime
  Settlement、Review或Authority；
- stdio无协议Session ID时只使用Praxis本地bounded Connection坐标，不能伪装成Provider身份；
- targeted ordinary×100与race×20通过。
- JSON-RPC Decoder、Tool Schema、canonical Call Arguments与Discovery Pagination Fuzz目标已
  落盘并分别运行5秒，均PASS。
- immutable MCP Execution Command与Protocol Receipt已增加transport-neutral API、Go SDK和
  CLI exact Inspect；targeted ordinary×100/race×20、64并发、deep-copy与Conformance通过。
  CLI只输出refs/digest/长度/状态/时间，不输出Params Inline或Canonical Response；NotFound
  保持未知语义，不被升级成未执行证明或重派许可。
- 首批Go benchmark（AMD 7840HS、Go 1.25.6、3轮）记录JSON-RPC Decode约10.5-10.8µs、
  Surface Compile约30.5-30.9µs、Registry Resolve约151-153ns；没有Rust门证据。

## 边界

本事件是owner-local `implementation_software_test_yes`。它不包含production Credential、Network
policy materializer、durable backend、host composition root、Application对generic Tool Owner
DomainResult/Settlement flow的总装、Task/Cancel/Drain/Close或SLA。
