# MCP三类发现材料闭环

- 时间：2026-07-18 16:29（Asia/Shanghai）
- 状态：`implementation_software_test_yes`（owner-local，不代表production GO）

## 结果

- official MCP SDK的Tools、Resources、Prompts分页返回现在都保留完整canonical JSON；
- 三类材料分别使用typed Observation与typed Material Ref，随同一Page Receipt/Observation在
  owner-local物理仓内原子提交；
- Page Receipt可通过三类exact Material Set Reader枚举对应Observation+Material Ref；
- Go SDK、transport-neutral API与模块内CLI只提供双读exact deep-copy Inspect；
- duplicate Tool/Prompt name、duplicate Resource URI、字段摘要漂移、wrong Ref、nil/canceled
  context、typed-nil、深拷贝篡改、lost reply与64并发均Fail Closed。

## 边界

- Resource不升级为Tool或Context事实；Prompt不升级为系统指令；
- 所有Provider材料都不授Authority、Review、Permit、Registry Admission或执行权；
- production durable backend、Snapshot provenance index、Tool Mapping Manifest、Resource/Prompt
  Context消费与能力启用仍未完成。

## 验证

- targeted ordinary ×100：PASS；
- targeted race ×20：PASS；
- full ordinary：PASS；
- full race：PASS；
- `go vet ./...`与`go mod tidy -diff`：PASS；
- Markdown relative links、drawio XML、trailing whitespace与`git diff --check`：PASS。
