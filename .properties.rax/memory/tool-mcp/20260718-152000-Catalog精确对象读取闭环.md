# Catalog精确对象读取闭环

- 时间：2026-07-18 15:20 CST
- 状态：`implementation_software_test_yes`
- 范围：transport-neutral Catalog对Capability、Tool、Package、Tool Alias提供
  `kind+ID+revision+digest` exact S1/S2读取，返回Record绑定的closed typed union与
  ProjectionDigest。
- 质量：覆盖wrong kind/digest、type-pun、S1/S2漂移、typed-nil、cancel、deep-copy、
  JSON黑盒、import Conformance与64并发确定性。
- 验证：定向ordinary×100、race×20、full ordinary、full race与vet全部PASS；gofmt、
  `go mod tidy -diff`和资产轻门随本批最终检查通过。
- 边界：只读投影不授Registry Admission、Authority、Review、Surface current或Provider执行权；
  没有CAS写口、网络协议、Webhook、数据库、production root或SLA。
