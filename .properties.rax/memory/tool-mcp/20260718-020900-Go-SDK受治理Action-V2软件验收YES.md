# Go SDK受治理Action V2软件验收YES

时间：2026-07-18 02:09 +08:00

## 结果

- 新增`SingleCallToolActionClientV2`，只依赖Application公开
  `SingleCallToolActionPortV2`与Owner clock。
- Execute/Inspect只接受已Seal且current的`SingleCallToolActionRequestV2`；SDK不会构造
  PendingAction、Authority、Settlement或Provider输入。
- Execute错误原样返回；Unavailable/Unknown不自动重试Execute，也不伪装NotFound。
- 返回Result以fresh S2重新执行`ValidateCurrentFor`，clock rollback、TTL crossing与digest
  drift全部Fail Closed。

## 实际门

- targeted ordinary x100：PASS。
- targeted race x20：PASS。
- 64同canonical并发最终Tool effect一次：PASS。

## 边界

这是owner-local SDK facade，不是production root、CLI call、网络API、Provider transport、
Credential、Capability启用或SLA。请求仍必须由Application/Harness/Model/Runtime公共链产生。
