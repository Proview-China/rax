# ToolPackage精确组装定向门通过、全门外部阻断

时间：2026-07-18 11:30 +08:00

## 结果

- Go SDK新增单一exact Registry Snapshot内的Package组装解析：只接受active Package，并逐项闭合exact Tool、exact Capability与active Registry Record。
- Package低报任一Tool Effect、inactive对象、旧Snapshot、exact依赖漂移、nil/canceled context与clock rollback均Fail Closed。
- 解析只组合现有Registry事实，不解析Alias，不Fetch、不验production签名信任、不Enable，也不产生运行时Authority。

## 实际验证

- targeted ordinary x100：PASS。
- targeted race x20：PASS。
- 64并发只读exact闭包：PASS。
- SDK full ordinary/race/vet：BLOCKED，原因是共享工作树中Runtime Owner在途文件`ports/checkpoint_restore_governance_v2.go`引用当前不存在的`CheckpointConsistencyRefV2.Ref/ManifestSeal`，导致`runtime/ports`编译失败；Tool Owner未越界修改。

## 状态

Package组装切片尚不标记full software-test YES；等待Runtime public ports恢复编译后复跑SDK full与Tool scoped full gates。production Package Fetch、信任根、透明日志、enable、revoke调度与Backend继续NO-GO。
