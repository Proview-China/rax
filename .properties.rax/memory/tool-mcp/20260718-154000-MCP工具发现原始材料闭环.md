# MCP工具发现原始材料闭环

- 时间：2026-07-18 15:40 +08:00
- 状态：`implementation_software_test_yes`
- 范围：Tool/MCP Owner-local，不代表production GO。

official MCP SDK的Tools page此前只保留摘要，schema、description和其他标准字段原文会丢失。
本次新增`MCPToolDiscoveryMaterialV1`，以exact Page Command、Connection、
`MCPToolObservationV2`和完整canonical Tool JSON保存Provider Observation；Page Receipt、摘要
Observation与材料集合在同一内存物理仓原子提交，并提供exact deep-copy Inspect。

同轮补齐Go SDK、transport-neutral API与模块内CLI
`mcp inspect --kind tool-material|tool-material-set --id/--revision/--digest`；Page Receipt可返回
排序唯一的Observation+Material Ref集合，调用方无需读取内部Physical Entry。三者只做S1/S2 exact读取，
typed-nil、nil/canceled context、source drift与deep-copy均Fail Closed。

材料不会自动升级为Capability、ToolDescriptor、Registry Admission、Review、Authority、Permit
或执行权。后续仍需用户冻结版本化语义Mapping Manifest；production durable backend、公开枚举
按Snapshot枚举索引与能力启用未完成。

验证覆盖canonical round-trip、字段摘要篡改、duplicate name、wrong exact Ref、nil context、
deep-copy、observed幂等、lost-reply inspect-only与64并发单Provider调用；full门结果见当轮报告。
