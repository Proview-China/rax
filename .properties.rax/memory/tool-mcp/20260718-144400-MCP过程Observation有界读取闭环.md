# 2026-07-18 MCP过程Observation有界读取闭环

- Tool/MCP Owner新增公共`MCPProcessObservationReadPortV1`：immutable exact Inspect与
  Connection full digest/Epoch/Snapshot/source-sequence有界pull-page；page limit为1..256。
- 唯一内存Journal同时维护history、每exact Connection sequence与每Connection+Snapshot有序索引；
  没有第二仓、网络、goroutine、Webhook或Provider入口。
- Go SDK、transport-neutral API及可嵌入CLI `mcp process`已经接入；只暴露有界摘要和digest，
  不输出raw notification data。
- process page不是Evidence、Timeline、ToolResult、Review或Authority；空页/NotFound不证明
  no-effect，Unknown/lost reply仍只沿原Attempt/Entry Inspect。
- owner-local实现与测试完成；production durable Journal、retention SLA、streaming follow、Task
  Watch和跨Owner总装仍NO-GO。
