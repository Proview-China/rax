# PD-TM-04第三次独立设计短审NO返修

时间：2026-07-16 18:32:30 +08:00

`PD-TM-04`第三次独立设计短审结论为NO，P0=1、P1=2、P2=1。本轮仅返修Tool Owner资产和反例，不写Go、不修改其他Owner。

返修冻结：Projection顶层Subject/requested必须分别exact等于Issuance Subject内重复字段，正数issuance requested严格约束Projection Expires；硬门覆盖Seal、Validate、Store create/read及issuance/exact两种Inspect，顶层0不得绕过issuance正数TTL。Candidate Current Closure新增PendingActionCurrent，必须来自同一Application Input/Harness current，并以ID/Revision/Digest/Owner/Checked/Expiry六坐标在S1/S2逐字段交叉；Candidate Validate不能替代。ClosureDigest canonical domain固定`praxis.tool`、version为Binding Current合同version、discriminator固定`SingleCallToolActionCandidateCurrentClosureV1`、body排除自身digest，并冻结required/nil语义。

真值同步：Application V2设计与Go公共合同已落盘、基础compile PASS，但测试和独立代码审计未完成；Runtime Association窄Reader尚未落盘。当前等待第四次独立设计短审，不自判YES，Tool Go门保持关闭。
