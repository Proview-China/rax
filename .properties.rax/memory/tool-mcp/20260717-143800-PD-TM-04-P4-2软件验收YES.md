# PD-TM-04 P4-2软件验收YES

## 事件

- Tool Owner在`ExecutionRuntime/tool-mcp`内完成P4-2 Owner-local切片：Capability/Tool Registry Object exact Current、Tool Input Contract immutable lease、ActionCandidateV3、BindingCurrentV2 durable root及其Reader/Store；
- 用户确认InputContract issuance采用稳定请求派生：ID只覆盖Application Request、PendingAction、Scope、Provider、Surface、CallName、Capability、Tool、Schema与requested坐标；Owner、完整Surface Entry、Artifact、LimitPolicy及current closure只进入单赢家immutable Projection；
- CandidateV3仅接受`N == 1`，并强制Model canonical arguments bytes/digest与Application/PendingAction payload、Candidate Payload exact一致；CandidateV3不成为独立current或第二恢复根；
- BindingV2 Reader按S1/S2 exact复读Application、Model、Surface Binding、Runtime Association/Generation/Route/Provider、Surface与Registry currents，完成共同TTL后才create-once持久BindingV2；BindingV2 Ref是P4唯一durable恢复根；
- nil/canceled context、typed-nil、Unavailable/NotFound分离、clock rollback、TTL crossing、same-ID drift、lost create reply、deep clone与64并发单winner反例已闭合。

## 验证

- targeted applicationadapter ordinary×100：PASS；
- targeted blackbox/conformance/fault ordinary×100：PASS；
- targeted applicationadapter race×20：PASS；
- targeted blackbox/conformance/fault race×20：PASS；
- `go test ./... -count=1`：PASS；
- `go test -race ./... -count=1`：PASS；
- `go vet ./...`：PASS。

## 当前门

- P4-2状态：`implementation_software_test_yes`；该YES只覆盖Tool Owner合同、Reader/Store与隔离测试；
- P4-0、P4-1、P4-2均已闭合Owner-local software test，但不代表Harness M2、跨Owneractual-point四读、完整P4、system或production GO；
- Tool仍不创建production composition root、Provider/Transport Backend、Credential、网络/RPC或持久Backend，也不启用Capability；
- 下一阶段必须由管理线单独授权Harness M2与每个attempt/Open/Stream/continuation actual-point接线；P5/system/production继续锁定。
