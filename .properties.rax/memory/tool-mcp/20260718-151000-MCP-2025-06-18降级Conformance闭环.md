# MCP 2025-06-18降级Conformance闭环

- 时间：2026-07-18 15:10 CST
- 状态：`implementation_software_test_yes`
- 范围：official Go SDK v1.6.1 public协议类型与Praxis严格JSON-RPC codec的
  initialize、tools/list、tools/call往返，以及stable链不放宽反例。
- 边界：正式受治理Connect/Discovery/Call继续锁`2025-11-25`。SDK没有public旧版本Client
  option，因此没有宣称真实public Session降级、production Transport、长期支持期限或SLA。
- 验证：定向ordinary×100、race×20、full ordinary、full race、vet、gofmt、`go mod tidy -diff`、
  import boundary、relative links、trailing whitespace与`git diff --check`全部PASS。
