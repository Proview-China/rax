# 2026-07-18 Tool Alias装配期解析闭环

- Tool Alias V1按业务文档固定为assembly-only：Owner+namespaced Alias映射active exact Tool；
  Surface/Run只持有Tool revision+digest，Run不读取moving Alias。
- 唯一`registry.Registry`维护Alias immutable history/current、revision 1 create、successor
  current+1 full-Ref CAS、revoke与全局Registry Snapshot；没有第二Alias仓。
- SDK提供Register/Inspect与exact Registry Snapshot S1/S2 Resolve，返回Alias+Tool因果闭包；
  Catalog/CLI支持`tool-alias` list与alias exact Inspect。
- targeted ordinary×100、race×20、full ordinary/race与vet通过；64 same canonical并发只产生一个
  Registry revision，history rollback/ABA、wrong target/expected、inactive/revoked与stale Snapshot拒绝。
- owner-local实现完成；Package Alias、semver range、runtime latest、production durable Registry、
  Assembler/Reconcile和production能力启用仍NO-GO。
