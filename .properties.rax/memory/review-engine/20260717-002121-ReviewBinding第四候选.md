# 2026-07-17 Review Binding 第四候选

## 事件

第三候选出现独立审计分歧，按更严格反方结论固定为`NO，P0/P1/P2=2/3/1`。本轮只修改Review自有design/plan/memory，未修改Runtime/Harness/其他Owner或任何Go代码。

## 第四候选收口

- Runtime Binding Owner候选同时冻结Review只读Reader与Owner-only Create/CAS Publisher；Review和composition不持写口。
- 首建Revision=1；续版严格+1；publish receipt、append-only history、highestRevision、current full-Ref CAS与相关底层mutation在同一Owner事务全有全无。
- mutation lost reply只能Inspect预先sealed exact PublishRef；NotFound/Unknown不授重新Create/CAS。
- host composition只注入sealed consumer association ref；request不能自报或discover association。
- Resolve与每次InspectCurrent复读Binding全closure、association current、Association.Consumer Binding/Capability current；association与consumer TTL进入true min。
- historical exact Inspect只依赖Ref/Source/Subject/digest，与current index/highest/closure解耦；纯时间跨TTL只使ValidateCurrent失败，不发布expired revision。
- hard negatives扩展为BIND-01..28，新增首建、staged zero-write、64 CAS、lost reply、consumer revoke、TTL、history/bad-current解耦与时间到期不增revision。

## 三轴状态

- Review owner-local aggregate：`YES 0/0/0`。
- Binding候选审计轴：第三候选`NO 2/3/1`；第四候选`READY`待双独立审，未自标YES。
- production：Binding P0=1、external总P0=6继续OPEN/NO-GO。

## 禁止项

- 未写Runtime/Go/Adapter/fixture/benchmark/root；
- 未stage/commit；
- 未宣称production readiness或运行Go测试。
