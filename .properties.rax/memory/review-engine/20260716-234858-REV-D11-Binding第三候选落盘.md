# 2026-07-16 REV-D11 Binding 第三候选落盘

## 事件

用户明确授权把聊天内第三候选落到Review自有design/plan/memory；Runtime/Harness/其他Owner与全部Go代码继续只读。本轮新增 `rev-d11-binding-authoritative-current-port-delta-v1.md`，并同步Review Port Delta、REV-D11主设计、测试矩阵、live依赖盘点、设计入口与plan入口。

## 冻结结论

- 唯一公共窄Delta候选由Runtime Binding Owner拥有：exact Source/Subject/ProjectionRef/Projection与Resolve、historical exact Inspect、current exact Inspect三方法；Review不得私建替代。
- `ReadAuthoritativeBindingClosure`不是第二个public Raw Fact Port，而是Resolve与每次InspectCurrent内部必须执行的Owner语义：同一snapshot复读当前BindingSet、全部member BindingFact、Set/Fact两侧完整Grant及全member TTL，并与history/current full Ref/highestRevision交叉验证。
- live `BindingFactPortV2`的分离Inspect不足以证明同一snapshot；可复用concrete Store、Validate与digest算法，但production实现必须有Owner内部snapshot capability，不能fallback。
- S1执行Resolve后立即current exact Inspect；S2只按S1保存的Ref+Source+Subject再次current exact Inspect，不重新Resolve。底层renew/revoke/drift但projection未推进时Fail Closed。
- true min TTL无条件覆盖Set、全部Facts和两侧全部Grants；返回projection deep clone，fixed Checked/Expires/ProjectionDigest，Unknown不降NotFound，lost Resolve reply后的新Ref只能开启新S1。
- BIND-01..17映射未来targeted ordinary100、race20、full ordinary/race与vet；其中14/15固定两个具名canonical input的literal golden，16/17拒绝字段改名、缺失与重复；当前无Go实现，未运行且未宣称通过。
- 候选单列的十个live输入SHA-256已逐项`sha256sum -c`回读一致；它们与原五Owner盘点的九文件指纹是两个明确的stale边界，不能混写。
- 首轮双独立审计发现两类资产一致性问题：Binding候选错误对与REV-D11聚合closed set不兼容；纯值`ValidateCurrent`越界声称可验证Owner index/highestRevision，且总合同未标注Binding Resolve的新S1特化。现已分别收敛为主合同已有error组合；以及“Owner Reader同snapshot证明实时current、纯值ValidateCurrent只验sealed值/expected/TTL”，并明确Resolve lost reply只能开启全新S1，S2/exact recovery不得Resolve。等待两名审计者基于稳定修正版重审。
- 第二独立审计的唯一P1是canonical body仍为描述性文字；现已增加具名`ReviewBindingSetExactRefV1`、`ReviewBindingProjectionIdentityInputV1`、`ReviewBindingAuthoritativeClosureInputV1`，冻结全部字段/JSON tags、重算职责、strict decode、两个literal JSON+digest golden及四项对应反例。Binding production门仍未因此关闭。
- stable复核又发现两处旧index职责措辞及strict decoder错误对未完全进入closed set；已机械统一为Owner current Reader验index、纯值ValidateCurrent验sealed expected/current/TTL，并加入`InvalidArgument/InvalidCanonicalForm`与`Conflict/DuplicateCanonicalKey`唯一oracle。
- 后续严格反方审计覆盖早先YES，第三候选最终审计轴固定为`NO 2/3/1`：缺Owner publisher原子闭环、缺host consumer association/current proof，以及history/纯到期/反例不足。本轮已形成第四候选：Owner-only Create/CAS/Publish Receipt exact恢复；receipt/history/highest/current与底层mutation原子；bound association及consumer Binding/Capability每次S1/S2复读并纳min TTL；historical与坏current解耦；纯时间到期不增Revision；矩阵扩至BIND-01..28。第四候选只写READY待双审，不自标YES。

## 双轴真值

- Review owner-local aggregate：**YES，P0/P1/P2=0/0/0**；此轴不受Binding候选审计轴污染。
- production external：**NO-GO，P0=6**（Binding/Evidence/Policy/Authority/Scope各1项，加composition/root 1项）。
- Binding production P0=1仍OPEN；第三候选审计NO 2/3/1，第四候选READY待双独立审，Runtime Owner尚未冻结/实现Reader/Publisher/association及production Backend/conformance。

## 未做事项

- 未修改Runtime/Harness/Application/Model Invoker或其他Owner资产；
- 未修改、创建或运行任何Go实现、fixture、benchmark、Adapter或production root；
- 未stage、commit或选择生产DB/RPC/拓扑/SLA。
