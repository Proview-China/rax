# 2026-07-16 Review Wave 1 独立复审修复

## 状态

**awaiting independent review**

## 粗粒度事件

Review Wave 1 按独立代码复审的 P0/P1/P2 清单完成限定修复：Auto Attestation 的 applied ApplySettlement/ReviewerInvocationResult exact 链、Verdict Owner current snapshot 与最短 TTL、Target-to-Case 唯一 create-once、全链 Target/Reviewer binding、append-only Case/Verdict history、mutation lost-reply exact Inspect、staged failure rollback，以及 Runtime V4 Reader fresh-clock/TTL crossing/rollback 均已进入 Review 独占实现与测试。

设计、验收、计划、模块说明和结构化 Port Delta 已同步。没有修改 Runtime、Harness、Application、Model Invoker、根配置、Go workspace 或 CI；没有 stage/commit，也没有创建 production composition root。

## 本轮新增关键反例

- same exact Target：同 Case canonical replay 幂等，换 Case ID或同 Case payload drift冲突；
- new Target revision：未 supersede 时拒绝，显式 supersede 后创建新 Case且旧 revision可 exact Inspect；
- Auto settlement：`not_applied`、`failed`、跨 Case/Round/Assignment/Target、Attempt/ResultDigest 漂移在 Attestation/Verdict 前失败关闭；
- staged compound mutation：Attestation、Decide、Invalidate 在 Trace source-sequence 冲突时 current/history 零泄漏；
- mutation reply-loss：Decide 和 Invalidate 各只调用一次 mutation，并 exact恢复 Case+Verdict；
- Runtime Reader：首次 Inspect/retry跨TTL、clock rollback、原 ctx取消后的 exact retry均失败关闭或正确恢复。

## 生产残余

REV-D11 仍阻塞 production Verdict path：Binding Owner 与 Evidence Owner 尚需冻结可注入的 public exact-current Reader/Projection，以及 ReviewEvidenceRef 到 authoritative Evidence Owner Fact/current applicability 的无损映射。当前 `DecisionExternalCurrentReaderV1` 只是 Review adapter seam，`internal/testkit` 实现不得进入生产。

独立 PolicyNotRequired current Fact/Reader 仍未闭合，因此 `runtimeadapter.ReaderV4` 对 `operation_not_required` 保持 Fail Closed/unsupported，不能用 accepted Verdict 伪造。

## 实际验证记录

在 `ExecutionRuntime/review` 执行：

```text
go test ./... -count=100 -run 'P0|TargetCaseUniqueness|StagedMutation|OwnerLostReplies|ConformanceWave1|FreshClockBoundaries|AttestationReplyLoss'  PASS
go test -race ./... -count=20 -run 'P0|TargetCaseUniqueness|StagedMutation|OwnerLostReplies|ConformanceWave1|FreshClockBoundaries|AttestationReplyLoss'  PASS
go test ./...  PASS
go test -race ./...  PASS
go vet ./...  PASS
gofmt -w .  PASS
gofmt -l .  PASS，空输出
```

在仓库根执行 Review scope `git diff --check` 与尾随空白检查均 PASS；source import 与 `go list -deps` 扫描均未发现 Application、Harness、Model Invoker 或 Runtime foundation/fakes/kernel 实现包依赖。
