# Context模型输入Lineage V1实现

## 结果

从`origin/main@a9481b2e35abc62ed60fcf92a2a009b49064106e`建立独立
`agent/context-model-input-lineage-v1`工作树，完成Context Owner additive
model-input lineage current reader。

已落地：

- 公共Kind `praxis.context/model-input-material`与`praxis.context/frame`；
- `Owner/Kind/ID/Revision/Digest` exact source；
- Material exact/current与Frame exact-current的完整S1/S2复读；
- history-only拒绝和request/Material/Frame/30秒上限TTL最小值；
- zero-projection的Unavailable、Unknown、cancel、clock rollback、TTL crossing；
- Owner/Kind/ID/Revision/Digest逐轴、互换、旧Ref reseal和64并发Frame flip反例。

## 验证

- 定向ordinary `count=100`：PASS；
- 定向race `count=20`：PASS；
- `go test ./...`：PASS；
- `go test -race ./...`：PASS；
- `go vet ./...`：PASS；
- `gofmt`与`git diff --check`：PASS。

## 边界

没有修改既有Material/Frame/runtimeapi摘要或合同，没有新增SQLite，没有调用或
修改Harness、Model Invoker、Provider。Frame exact-current目前由Context public
Port注入，仓库只提供线程安全测试fixture；production Frame backend、
composition root及下游dispatch继续NO-GO。
