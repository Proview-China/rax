# Recipe Compare离线入口完成

时间：2026-07-17 15:10:00 +08:00

Context Owner在既有`ContextRecipeComparisonV1`确定性内核之上完成第五个Offline Engineering只读入口：`CompareRecipesV1`、strict JSON request/response codec、`offlineapi` typed/JSON dispatch及`context recipe compare` stdin/stdout CLI。

边界：输入只含两个exact ContextRecipe和Checked/Expires；`MaxRecipes=2`，wire为48/48 MiB。Checked必须同时位于两个Recipe有效期内，报告Expires不得延长任一Recipe TTL。输出只含规范排序field-path change与before/after digest；不返回正文，不作better/compatible/Review/publish/rollback结论，不持有Store，不注册Capability。

反例覆盖：递归duplicate JSON key、null Recipe、MaxRecipes不足、Request/Comparison/Result digest漂移、TTL延长、cancel零Response、caller input/response pointer no-alias、相同Recipe的present empty `changes:[]`及64并发确定性。

实际验证：

- `go test -count=100 ./sdk ./kernel ./offlineapi ./cmd/context`：PASS；
- `go test -race -count=20 ./sdk ./kernel ./offlineapi ./cmd/context`：PASS；
- `go test -count=1 ./...`：PASS；
- `go test -race -count=1 ./...`：PASS；
- `go vet ./...`：PASS。

当前Go闭集hash：`8e833068418b3b95ed8d9c00165f9f65d743234f34700517ab1095f6525b3274`。production发布、跨Owner root、Capability、Harness Continuation与Turn推进仍NO-GO。
