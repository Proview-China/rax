# PromptAsset内嵌片段裁决

时间：2026-07-17 22:55（Asia/Shanghai）

用户选择PromptAsset方案1：权威资产直接保存规范化片段规格与exact ContentRef，不新增独立PromptFragment Fact，也不把Prompt降格为无版本Recipe Source。

V1片段角色冻结为provider-neutral `instruction/example/policy`，分别确定性映射Context已有Kind/Trust；PromptAsset不保存Provider messages、不决定Frame Region、最终顺序或Cache placement。Recipe继续拥有Admission与布局。资产声明Owner/Authority/Sensitivity、ContentDigest、exact RenderCompatibility refs、Evidence和TTL。

Owner-local实现范围为immutable Put/Inspect、distinct pre-release lifecycle-head CAS、lost-reply Inspect和exact Asset→Candidate projection。production publish/rollback/revoke、Review/Runtime接线、Capability与production root仍由CTX-D07和跨Owner公共合同门禁，不因该裁决解锁。
