# Harness公共合同与最小基座完成

时间：2026-07-14 15:57（Asia/Shanghai）

## 粗粒度事件

用户确认沿“公共合同→最小可运行骨架→Port接入→完整闭环验收”的路径继续后，Harness首个组件中立切面已经完成。

## 已落地

- 冻结不可变`BootstrapPlan`、`Manifest`、Run State、Event、Action与Completion Claim；
- 建立Context、Model Turn、Event Candidate三个窄Port；
- 实现每Endpoint单活跃Run Interaction Loop、source sequence、Action/Input等待、取消、事件背压和迟到结果隔离；
- 通过现有Runtime `ExecutionPort`完成Describe/Preflight/Open/Inspect/Control/Close接线；
- Runtime Foundation真实完成Activation、Ready、Runtime Run关联、Harness直接完成/Action Gateway续跑、Stop与Cleanup；
- 保留Runtime Run Record/Execution Outcome与Harness Claim的事实所有权边界；
- 明确首切面不暴露Checkpoint，不执行真实Tool/MCP，不包含具体生产Harness。

## 验证结果

`ExecutionRuntime/runtime`、`ExecutionRuntime/harness`和`ExecutionRuntime/model-invoker`分别执行：

- `go test -count=1 ./...`：通过；
- `go test -count=1 -race ./...`：通过；
- `go vet ./...`：通过。

## 后续入口

下一步由用户选择具体组件。若选择具体Harness，需要先确定Route、Conformance、ModelTurn桥接、进程/会话协议、Secret与Effect边界，再接入当前公共骨架。Harness Completion Claim到Runtime Run Outcome的生产级验真与结算协调者仍需单独设计。
