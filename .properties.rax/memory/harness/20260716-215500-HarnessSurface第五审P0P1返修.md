# Harness Surface第五审P0/P1返修

## 事件

- Surface第五审结论为`NO(P0=4/P1=2)`；Harness仅形成资产返修候选，Go继续`NO-GO`；
- Registry统一为候选`runtimeports.RegistrySnapshotRefV1`，Owner必须是完整`core.OwnerRef`；删除Harness私有Registry nominal与旧Model名；
- Runtime neutral Assembly Current Ref/Projection均完整闭合Generation、Handoff、BindingSet、Manifest、Conformance、Watermark、ToolSurface、Profile、Registry、Semantic、Currentness、Checked、Expires与ProjectionDigest；own digest不进入自身canonical body；
- stored ACK恢复仍以`InspectByPreparedCurrent`作为第一项Owner调用；命中后必须复读Prepared Current并取fresh Harness Owner clock验证ACK+Current freshness，成功才返回clone。该路径零Tool、零Ensure、零重Seal，但不允许零clock；
- Runtime ports neutral Port Delta尚未落盘并通过compile，仍是external P0。

## 状态

等待Surface第五审返修独立复审；本事件不授权Harness Go、Tool P4、system G6A、Capability或production root。
