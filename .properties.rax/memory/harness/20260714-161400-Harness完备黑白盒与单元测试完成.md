# Harness完备黑白盒与单元测试完成

时间：2026-07-14 16:14（Asia/Shanghai）

## 粗粒度事件

在Harness公共合同与最小基座完成后，用户要求继续补齐完备的黑盒、白盒和单元测试，并在完成后停止。该测试切面已经完成，未进入任何后续公用组件实现。

## 测试分层

- 单元测试：`contract`、`ports`、`fakes`包内表驱动测试；
- 白盒测试：`kernel`和`runtimeadapter`包内状态、故障、上限、锁隔离与私有帮助函数；
- 黑盒测试：只通过公开API运行Harness direct、Action Gateway、Input continuation、Cancel和Runtime Foundation生命周期。

共39个顶层测试。生产包使用`-coverpkg`合并统计后的语句覆盖率为89.7%。

## 关键反例

- 配置/证据/Conformance/摘要漂移全部在外部调用前拒绝；
- Opaque Payload内容替换、错误Schema、非法JSON与解码丢字段拒绝；
- 未持久化Intent、过期Fence与Control缺失Fence拒绝；
- Context/Model/Event失败、Turn/Event上限均不能形成虚假成功；
- 同Scope双Run、Run ID复用、错误ActionRef、终态继续输入拒绝；
- Cancel可中断阻塞Model调用，迟到结果不能复活Run；
- 不同ExecutionScope并发隔离，关闭/陈旧/异属Endpoint被Fence；
- fully/restricted两种Conformance的Runtime完整闭环通过。

## 实际验证

Harness：

- `go test -count=1 -shuffle=on ./...`：通过；
- `go test -count=20 ./...`：通过；
- `go test -count=1 -race -shuffle=on ./...`：通过；
- `go vet ./...`：通过；
- 跨包覆盖率：89.7%。

回归：

- `ExecutionRuntime/runtime`普通、Race、Vet：通过；
- `ExecutionRuntime/model-invoker`普通、Race、Vet：通过。

## 停止点

Harness公共基座及其测试切面到此停止。下一步等待用户开始逐一设计和实现6+公用组件，不提前扩张范围。
