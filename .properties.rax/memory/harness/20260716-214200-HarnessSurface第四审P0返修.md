# Harness Surface第四审P0返修

## 事件

- Model Gate只保留公开短方法`Commit + InspectExactAck`，Harness design/plan不再使用旧长方法名；
- Harness/host成为Model ACK Repository Owner。同一concrete实例提供Owner-local `EnsureAck`、公开Gate `InspectExactAck`及internal `InspectByPreparedCurrent`，stable key完整绑定Prepared+Current；
- Tool Binding仓与Model ACK仓不宣称跨仓原子。重入第一项Owner调用先按完整Prepared+Current执行`InspectByPreparedCurrent`；命中返回stored clone，只有同实例`by_prepared_current + by_prepared_ref`权威证明never-created后才恢复Tool winner、取fresh clock并Ensure同canonical Model ACK；Unavailable/Indeterminate只Inspect原key；
- Tool Exact Reader固定由Model ACK公开neutral SurfaceBindingRef单参可达；Harness只无损映射Owner/Contract/ID/Revision/Digest，不按Kind猜源；
- live DAG下Harness Gate将依赖Tool公开Port，Tool反向import Harness assemblycontract会形成module SCC，因此拒绝该依赖。唯一neutral `ModelPreDispatchAssemblyCurrentRefV1/ProjectionV1/ReaderV1`须由Runtime ports承载类型，Harness拥有semantic/publisher/current并返回该类型，Tool Binding直接嵌入；禁止Tool echo、Kind、别名或重签；
- Memory/Knowledge第二套DTO已撤但G6B仍P0。Context是TransitionProof唯一Owner，Application只协调；SourceTurn full exact ref来自Session/Turn Owner Reader，Harness不接SourceTurn/Proof，只消费final TargetTurn current Frame；首个G6B保持Sources=0、Reader calls=0。

## 状态

Harness侧桥接资产已对齐；当前唯一external P0是Runtime ports neutral Port Delta尚未落盘并通过compile。该事件不表示联合复审YES，也不授权Go、Tool P4、system G6A、Capability或production root。
