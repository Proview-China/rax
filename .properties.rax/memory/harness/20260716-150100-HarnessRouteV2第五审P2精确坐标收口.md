# Harness Route V2第五审P2精确坐标收口

时间：2026-07-16 15:01（Asia/Shanghai）

live复核确认Conflict provenance与Legacy closed-state/absence proof已存在，但AliasSurface的`Validate`此前只校验Kind闭集和字段格式，未按Kind强制坐标形状；Dependency surface还重复记录FromRef而遗漏ToRef。

本轮补齐Candidate、Port、Slot、Factory、Dependency、Phase六类closed坐标校验。Dependency现在以`Ref=FromRef`、`PortSpecRef=ToRef`绑定双端；Conflict的exact `AssemblyInputDigest`继续绑定完整offending input，AliasSurface只负责在该输入内唯一定位来源。新增反例同时覆盖postbinding缺AssemblyInput/Graph/Wiring、code/phase互换、未知Legacy state、state/Active漂移、缺sealed wiring proof和各Kind字段错位。

状态保持等待Harness Route V2终审；终审YES前不继续G6A Go，不启用cross-module fixture、生产composition root或Capability。
