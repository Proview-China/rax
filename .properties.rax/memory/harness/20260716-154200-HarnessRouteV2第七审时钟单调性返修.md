# Harness Route V2第七审时钟单调性返修

时间：2026-07-16 15:42 CST

## 事件

- Harness Route V2第七独立短审结论为`NO(P0=0/P1=1/P2=1)`；第八最小返修只处理Legacy Route S1/S2跨读取时钟单调性与fresh覆盖率口径。
- `inspectLegacyRoutesCurrentV2`在取得`nowS1/nowS2`后显式要求`nowS2>=nowS1`；否则返回`core.ReasonClockRegression`并Fail Closed。
- 新反例使用同一sealed Fact与`checked=T0/expires=T0+60s`，时钟依次为`T0+50s/T0+10s`；两次分别都在租约窗口内，但跨读取发生回拨，必须拒绝。既有`now<checked`反例保留。

## 验证真值

- Route六包ordinary `count=100`与race `count=20`通过；Harness full ordinary/race/vet通过。
- fresh覆盖率命令固定为`go test -count=1 -coverpkg=./... -coverprofile=<temp> ./...`，再以`go tool cover -func=<temp>`读取total；本次实测为`75.2%`。
- 旧memory保持历史原文；本条与current module/ExecutionRuntime README承载新口径真值。

## 边界

- 未继续G6A、未修改其他Owner、未提供production root/Backend/SLA。
- 第八返修只提交第八短审，不自判终审`YES`。
