# Sandbox Snapshot purge第二候选独立审计返修

- 时间：2026-07-16 23:26 CST
- 状态：`review_pending / implementation-NO-GO`
- 审计输入：owner-local `P0=3 / P1=2 / P2=1`，external `P0=3`
- 范围：仅Sandbox design/plan/memory资产；未写Go，未修改Runtime/Management，未stage/commit。

## 保持不回退

- delete request不等于deleted；
- `praxis.sandbox/snapshot-artifact-purge`仍是唯一物理删除Effect；
- live `OperationSettlementCurrentReaderV5`仍是Checkpoint-only一方法，Snapshot purge只提additive
  sibling Port Delta。

## P0返修

1. Retention候选补齐`LegalHoldIndexExactRefV1`、
   `ExpectedLegalHoldIndexCurrentRefV1`、`LegalHoldCoverageCarryExactRefV1`、
   `NoActiveLegalHoldProjectionExactRefV1`及Carry From/To Index full exact refs；冻结current/history、
   expected absent/present、权威NotFound与lost-reply同read key规则。
2. Runtime sibling不再引用`PurgeRequestRefV1`、`ArtifactSubjectRefV2`、
   `ExpectedAggregateRefV2`、`DeletionAttemptRefV2`等Sandbox nominal。候选改为Runtime-owned
   `OperationDomainFactExactRefV1`与neutral BindingSet；Sandbox未来只在`runtimeadapter`逐字段无损
   单向映射。Runtime→Sandbox依赖、复制DTO、双向adapter与SCC全部禁止。
3. Purge Evidence唯一Owner冻结为Runtime Evidence Owner；Qualification Issue、Provider Handoff、
   Ledger Record、Consumption分别绑定Operation/Effect/Permit/DispatchAttempt/prepare或execute phase
   exact refs。prepare/execute对象不可互填；Issue/Record/Consume lost reply只Inspect原write key。

## P1返修

1. Retention四Reader逐方法冻结closed Category/Reason、NotFound权威性、Unknown/Unavailable和
   zero-result语义。NoActive current的NotFound绝不等于NoActive；过期history只可审计。
2. purge cleanup冻结独立custom Operation、Effect/Attempt、Evidence、Settlement V5、one-method
   current Reader/Gateway marker与Sandbox Apply closure。Apply只更新CleanupFact/Residual，不推进
   deleted、purge indeterminate或Runtime Outcome。

## P2返修

- `acceptance.md`重复SA13/SA14已修为唯一连续`SA01..SA19`；plan/test matrix同步neutral DTO、
  Evidence、Retention Reader与cleanup反例。

## 第二审账本

- Sandbox owner-local：`P0/P1/P2=0/0/0`；
- external P0：`3`，不减——Retention公共合同、Runtime public Evidence/Settlement/Reader/Gateway、
  Management CurrentIndex/Tombstone最终DTO与持久化/续期；
- 结论：第二候选可提交独立审计，整体继续NO-GO。Provider、purge/cleanup执行、production
  backend/root与Go实现均为0。
