# Sandbox 生产接线最终收口

时间：2026-07-23 21:33 CST

状态：`sandbox-owned-production-wiring-complete / external-production-certification-pending`

## 本次完成

- Go Owner Core、SQLite State Plane、Runtime/Application Adapter、SDK/API/CLI 与
  Production Host Root 已形成可运行接线。
- Rust Data Plane 已具备 durable journal、actual-point current enforcement、Host
  bwrap、containerd/OCI、QEMU/KVM、Wasmtime/WIT 与 Remote neutral connector。
- Workspace overlay/change-set、真实 commit、Checkpoint artifact、canonical Snapshot
  bundle、AES-GCM content store 与 fresh Instance Restore stage 已闭合。
- Host Root 提供独立 `/livez`、`/readyz`；关闭后 readiness fail closed。
- `snapshot_artifact_capture_v2=true`；依赖外部 Retention/Legal Hold 与 governed purge
  的 terminal `snapshot_artifact_owner_v2=false`，没有越权宣称。
- 清理 README、design、plan、module 的陈旧实现状态；架构图 XML 可解析。

## 实际验证

| 门 | 命令 | 结果 |
|---|---|---|
| Go 重复 | `go test -count=100 -shuffle=on ./...` | PASS |
| Go 高并发 Race | `go test -count=20 -race -shuffle=on ./...` | PASS |
| Go 普通 | `go test ./...` | PASS |
| Go Race | `go test -race ./...` | PASS |
| Go 静态 | `go vet ./...` | PASS |
| Go 格式 | `rg --files -g '*.go' -0 \| xargs -0 gofmt -l` | PASS，零输出 |
| Rust 全目标 | `cargo test --all-targets` | PASS |
| Rust 严格静态 | `cargo clippy --all-targets --all-features -- -D warnings` | PASS |
| Rust 格式 | `cargo fmt --all -- --check` | PASS |
| Host/Wasmtime | 普通 Rust 测试中的真实 bwrap 与 Wasmtime cases | PASS |
| QEMU/KVM | `PRAXIS_TEST_KERNEL=/tmp/praxis-sandbox-test-kernel cargo test --test microvm_backend -- --ignored --nocapture` | PASS，2/2 |
| containerd/OCI | 以 root 执行已构建的 `containerd_backend-* --ignored --nocapture` | PASS，1/1 |
| 文档差异 | `git diff --check --` 限定 Sandbox 代码与资产目录 | PASS |
| 图 | Python `ElementTree.parse(architecture.drawio)` | PASS，`XML_OK` |

containerd 仅为实测临时启动，测试后已恢复 `inactive`；临时 KVM kernel 副本已删除。
未 stage、未 commit，未修改其他 Owner 的实现。

## 仍由外部 Owner 关闭的生产门

1. Retention/Legal Hold sealed current proof；
2. Runtime Snapshot purge/cleanup sibling 与 Management terminal DTO；
3. Agent Host 最终 factory/provider/phase registry；
4. deployment attestation、Certification Fact、镜像/密钥/guest artifact 供应链；
5. vendor Remote connector、分布式 State Plane、升级与 SLA 认证。

以上不是 Sandbox Owner 可以私建或自证的合同。它们不否定当前 Sandbox 自有代码与真实
backend 的完成状态，但在联合 certification 前，Component Release 仍只能发布
`standalone`，不能标记完整跨 Owner production。
