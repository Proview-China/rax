# Sandbox Snapshot purge第三候选复审返修

时间：2026-07-16 23:42:38 +0800
状态：`review_pending / implementation-NO-GO`

## 审计输入

- 第二候选复审：`P0=2 / P1=1`；external P0仍为`3`。
- 已闭合且保持不变：Retention全部full exact refs/Reader errors、`SA01..SA19`唯一编号、
  delete request不等于deleted、purge唯一物理删除Effect、Checkpoint V5不扩义。

## 本轮纠偏

1. Runtime neutral DTO改为`OperationDomainOpaqueExactRefV1`：只含role、canonical bytes/length、
   source canonical digest与Runtime neutral envelope digest。它不要求Sandbox Subject/Aggregate并不存在的
   Owner、Kind、Revision、Schema或Expires；Sandbox runtimeadapter必须byte-exact round-trip，禁止默认填充。
2. Purge Evidence全部exact Ref补齐ContractVersion、TypeURL、DigestDomain、Digest与TTL；Provider
   Handoff补current projection和最短TTL。公共写口改为同一Runtime Evidence Owner的atomic
   `RecordAndConsume`：Record、Chain、Cursor、Consumption与Qualification-consumed marker一个CAS提交，
   部分写入不满足Settlement资格；所有Port补齐Request/Result shape。
3. purge cleanup独立合同补齐Sandbox exact source、neutral CleanupBindingSet、ProviderHandoff、两阶段
   EvidenceBindingSet、Evidence atomic commit、Settlement Submission/Association/Guard/Projection/
   EffectTerminal/CommitBundle/Inspection以及current Reader的字段、canonical domain、presence和最短TTL。
   purge/cleanup互填、TTL延长、占位对象和partial commit均fail closed。

## 当前真值

- Sandbox owner-local候选：`P0/P1/P2=0/0/0`，等待第三次独立审计。
- external P0=`3`：Retention/Legal Hold公共合同、Runtime Snapshot purge sibling、Management最终
  CurrentIndex/Tombstone仍须各Owner冻结并落地。
- 未写Go、未修改Runtime/Retention/Management、未stage/commit；Provider调用仍为0。
