# Sandbox Wave1实现与验收资产收口

时间：2026-07-16 11:25:40（Asia/Shanghai）

状态：`wave1-completed`。本事件只同步Sandbox独占资产，没有修改Go代码、Runtime、
Harness、Application、Continuity、Model Invoker、全局索引、Go workspace或CI。

## 事件

复读`ExecutionRuntime/sandbox` live代码和测试后，确认Wave1已经存在并可编译：

- Sandbox Owner `FactStore`与只读`ExactCurrentStore`；
- backend-neutral `BackendConformancePort`与test-only本地Conformance；
- Requirement/Policy、Backend capability/risk、Placement及Workspace
  View/Overlay/ChangeSet纯模型；
- Reservation→Observation/Evidence refs→Inspection→`SandboxDomainResultFact`→
  Runtime opaque Settlement Ref→Domain `ApplySettlement` expected-revision CAS状态核；
- Runtime Enforcement 4.1 exact-current Reader，独立复读Attempt、Reservation、
  RuntimeLeaseBinding、Projection、Requirement、Policy、Placement、Backend、Slot与
  Generation Binding，绑定最短TTL并返回sealed projection；
- UnknownOutcome只Inspect、历史终态shape/current分离、并发CAS/lost reply和
  零Provider调用反例。

同步修正了Sandbox design/plan中“实现代码尚未授权”“当前没有Sandbox代码”“所有
文件均不会创建”等陈旧表述，明确区分Wave1 live范围与未来v2/production计划；新增
`.properties.rax/module/sandbox/README.md`作为不读代码即可理解的模块入口。

## 实际验证

在`ExecutionRuntime/sandbox`执行：

- `go test -count=1 -shuffle=on ./...`：PASS；
- `go test -count=100 -shuffle=on ./...`：PASS；
- `go test -race -count=20 -shuffle=on ./...`：PASS；
- `go vet ./...`：PASS；
- 无写入`gofmt -l`检查：PASS；
- `go list`包依赖与禁止import扫描：PASS；`contract/kernel/ports`零Runtime导入，
  全模块零Runtime内部、Harness、Application、Continuity、Model Invoker导入；
- Sandbox限定范围`git diff --check`：PASS。

上述结果只证明Wave1纯领域核、4.1 current Reader与test-only Conformance，不是生产
Backend、持久State Plane、production root或SLA证据。

## 保留NO-GO边界

- `SnapshotArtifactOwnerV2`仍只有设计边界，没有实现或backend handle；
- Checkpoint Participant prepare/commit/abort/restore、Compatibility/Restore V2及
  CheckpointRestore专用公共合同Sandbox映射全部未实现；
- live support matrix保持`FeatureCheckpointRestore=false`；
- 真实Provider/Enforcer、Host/Container/MicroVM/WASM production Backend、
  production composition root、真实Workspace commit、Assembly/SDK/CLI/API均未实现；
- Provider调用固定为0，不能以legacy EnvironmentPort、Evidence V3、Settlement V4、
  activation scope或testkit Fake绕过。

后续必须等待Runtime/Assembly公共合同代码、Owner联合Review和新的用户实现授权，
再按exact current/CAS/TTL/零调用反例优先的顺序进入下一切片。
