# Delta 10/11第八审双轴与V2精确Schema

时间：2026-07-16 22:16 CST

## 当前真值

- Owner-local：P0=0、P1=1、P2=0。
- External：P0=5，依次为具名Turn exact Ref/Reader、Context-owned TransitionProof exact nominal、Application三阶段namespaced Port、两Owner Adapter/nonzero/root、`knowledge_reference`完整exact source chain。
- live V1继续是唯一实现真值；本轮没有Go、Adapter、production root或远程Retrieval授权。

## 唯一Owner P1资产

- Memory与Knowledge各七个V2 public struct已经逐字段写明Go type、JSON tag与声明/canonical顺序。
- 两个`CurrentProjectionV2`已展开Memory `Query/View/Watermark`、Knowledge `Query/View/Snapshot/Pointer`、Authority/Policy/Purpose/Scope/License/Sensitivity、全部显式set digest、预算与fresh字段，不再保留占位语。
- StableClosureDigest V2已经写明两Owner独立digest domain、contract version、ObjectKind常量，以及逐字段canonical body和item body。
- live V1 ClosureDigest包含`ExpiresAt`，不得复用；V2精确算法为Validate/normalize后调用`contract.Digest(body)`，不使用`MustDigest`。

## 不重开项

stable/fresh分层、Owner evidence持久化/no-migration、AssociationDigest、ctx-aware bounded cancel和diagnostics已经第八审通过，不再作为Owner缺口。唯一Owner P1等待第八审对本轮精确schema复核；External P0保持5，Memory/Knowledge sources与Reader调用数仍为0。
