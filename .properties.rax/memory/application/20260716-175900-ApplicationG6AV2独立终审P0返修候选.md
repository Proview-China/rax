# Application G6A V2独立终审P0返修候选

时间：2026-07-16 17:59（Asia/Shanghai）

## 事件

Application V2第一次正式独立终审为`NO(P0=4/P1=2/P2=1)`；第二次短审仍发现版本冲突键等缺口，随后又收到Model与Tool Owner逐项裁决。四份设计资产已继续修订，当前状态仍为`独立终审NO / P0返修中`，等待下一次独立短审。本事件没有写Go、stage或commit。

## 本轮返修

- Binding只携IdentityRef；Harness Adapter通过公开`SettledTurnDomainResultReaderV3.InspectExact`读取full Identity；
- Harness Adapter额外使用Model根包唯一窄Reader`ToolCallCandidateObservationProjectionReaderV1.InspectExactProjectionV1`读取immutable historical Projection，原样携受限canonical arguments bytes，不伪造TTL；
- ProjectionProof canonical arguments是neutral proof唯一允许的bytes，最大长度复用`runtimeports.MaxOpaqueInlineBytes`，Reader Adapter/Seal/Clone/返回路径必须deep-copy，digest只对Model Reader原bytes执行`core.DigestBytes`；其余payload bytes仍禁止；
- Action V2及drawio同步删除Workflow、Observation、Assembly、Route、ParentFrame及Applicability等非必要V1重复坐标，只保留Scope+PendingSubject；
- Authority与Current全部S2后才取fresh nowS2、计算TTL并只Seal Request一次；
- Tool Owner原始Result ID/revision/digest与Application ResultCoordinate分域；
- Coordination增加跨版本stable semantic key；VersionClaim与revision=1 prepared Fact由同一Owner/Store在同一原子线性点create-once，Claim closed version、ConflictKey、Coordination ID、初始Fact digest与Created逐项exact，Create/Inspect/CAS每次复验；并固定CAS waiting_inspect成功回包后才有一次Execute权；
- Fact ID/Scope/Request/Created/revision/ResultRef immutable/exact关系已写死；
- Tool Binding exact current公共Port仍待Tool Owner Delta，P4/system继续阻断。

## 依赖真值

Harness Route V2及Owner-current V3/V4独立代码审计已YES并通过ordinary100/race20/full机械门；这只解除Harness依赖，不解除Application、Tool consumer、system G6A或production root。
