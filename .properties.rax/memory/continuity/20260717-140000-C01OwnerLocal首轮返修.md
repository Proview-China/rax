# C-01 owner-local首轮返修

时间：2026-07-17 14:00（Asia/Shanghai）

## 当前事实

- Runtime R-CTY-06 `EvidenceSubjectCurrentReaderV1` public ports/Gateway/Conformance已有软件实现验收YES；Continuity不再把它写成“合同未落地”，但Runtime production persistence/root/SLA仍不由此成立。
- Continuity Cursor解码新增strict canonical wire bytes校验；摘要正确但JSON whitespace/重编码不同同样Fail Closed，并新增fuzz资产。
- reference Rebuild从bulk `ReplaceLedgerScope`改为逐项`PutProjection`，不会因输入子集删除同Scope历史或Tombstone overlay。
- public `TimelineProjectionStore`删除`ReplaceLedgerScope`与`TombstoneProjection`；Tombstone改为immutable revision-1 `TimelineProjectionTombstoneFactV1`与visibility overlay，historical Event exact Inspect的revision/bytes保持不变。
- C-01仍非production GO：`Timeline.Project`与caller Candidate `Rebuild`尚未收拢到create-once Attempt、Runtime S1/S2/fresh current、按需typed Owner current及atomic Event+Attempt+current index Controller。

## 验证

实际通过：

```bash
go test ./contract -run 'TestQualityCursorRejectsNonCanonicalWireBytes|FuzzDecodeTimelineCursorCanonicalRoundTrip' -count=100
go test ./contract -run '^FuzzDecodeTimelineCursorCanonicalRoundTrip$' -fuzz '^FuzzDecodeTimelineCursorCanonicalRoundTrip$' -fuzztime=5s
go test ./domain ./storage/memory ./tests/blackbox ./tests/conformance -run 'TestProjectionRebuildRejectsCycleAndNeverReplacesHistory|TestTimelineTombstoneConcurrentConflictKeepsHistoricalEventImmutable|TestWave1TimelineAndContentPublicBehavior|TestTimelineStoreHasNoLegacyBulkReplaceOrEventMutationWrite' -count=100
go test -race ./storage/memory ./tests/blackbox ./tests/conformance -run 'TestTimelineTombstoneConcurrentConflictKeepsHistoricalEventImmutable|TestWave1TimelineAndContentPublicBehavior|TestTimelineStoreHasNoLegacyBulkReplaceOrEventMutationWrite' -count=20
go test ./... -count=1
go test -count=1 -shuffle=on ./...
go test -race ./...
go vet ./...
gofmt -l .
git diff --check -- .
```

结果：全部PASS；full ordinary、full race、vet、gofmt与diff-check均通过，`gofmt -l .`无输出；fuzz约执行28万输入。一次测试编译先暴露未使用局部变量，另一次暴露含slice结构不可直接比较，均已修正后重跑通过。

## 下一步

1. Continuity Runtime Adapter只注入R-CTY-06窄Reader，不注入raw Runtime Fact Port。
2. 实现Timeline Projection Attempt create/Inspect/CAS与atomic Event+Attempt+current index。
3. 非authoritative五类Trust只保持Ledger原值；`authoritative_fact`等待Assembler/领域Owner typed current Reader routing闭合。
