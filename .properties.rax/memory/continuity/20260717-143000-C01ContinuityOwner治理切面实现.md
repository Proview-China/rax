# C-01 Continuity Owner治理切面实现

时间：2026-07-17 14:30 CST

## 当前真值

C-01 Continuity Owner代码切面已完成。Runtime R-CTY-06 public Reader的软件实现通过公开`core/ports`被消费；Continuity没有导入Runtime kernel/control/foundation/fakes/internal，也没有修改Runtime、Harness、Application或其他Owner。

已实现：

- coordinate-only `TimelineProjectionRequestV1`与create-once Attempt、exact history/current Inspect、revision CAS、same-ID幂等、changed Conflict、no-ABA和`reconcile_required`；
- Runtime Evidence `InspectBySource -> InspectRecord`双读、R-CTY subject-current的S1/S2 exact比较与fresh Validate；
- 六类`EvidenceTrustClassV2`闭路由。五类Observation形态不调用Owner Reader；`authoritative_fact`必须走typed Owner Router，缺失时`unsupported`且Event为零；
- Projection Policy与按需Owner自然TTL取共同最小值，`RequestedNotAfter`只在最后缩短；
- Event+visible Attempt+Continuity current index原子提交；lost publish reply后只Inspect原Attempt，不能重发；
- Rebuild逐Request走同一Project治理链；旧caller Candidate服务改名`ReferenceTimeline`，明确只用于测试/reference；
- immutable Tombstone Fact+overlay继续保持historical Event revision/digest/bytes不变。

## 实际验证

在`ExecutionRuntime/continuity`运行并通过：

```bash
go test ./runtimeadapter -run '^TestTimelineProjectionAdapterV1' -count=100
go test -race ./runtimeadapter -run '^TestTimelineProjectionAdapterV1' -count=20
go test ./contract ./domain ./storage/memory ./runtimeadapter -count=100
go test ./... -count=1
go test -race ./... -count=1
go vet ./...
gofmt -l .
go list -deps ./runtimeadapter | rg 'ExecutionRuntime/runtime/(foundation|fakes|kernel|control|internal)|ExecutionRuntime/(harness|model-invoker)'
```

结果：ordinary×100 PASS、race×20 PASS、全模块ordinary/race PASS、vet PASS、`gofmt -l`与禁止依赖扫描均无输出。

## 未解锁

- authoritative领域的真实typed Owner current Readers与Application/Assembler binding；
- production SQLite/RocksDB/root、远程对象库、SLA；
- 跨Owner Checkpoint capture/Participant/Consistency；
- Restore Stage/Activate、Provider调用及外部世界回滚。

因此本事件是`C-01 Continuity Owner slice implemented`，不是production root或端到端Continuity完成声明。
