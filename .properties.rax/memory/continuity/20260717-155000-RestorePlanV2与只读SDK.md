# RestorePlan V2与只读SDK

时间：2026-07-17 15:50（Asia/Shanghai）

## 已完成

- 新增Continuity Owner `RestorePlanFactV2`：exact CheckpointConsistency/ManifestSeal/frozen-set、source Instance、fresh Instance/higher Epoch/new Lease/Fence proposal、Context与compatibility/currentness/review/authority/budget/binding requirement refs、stable tenant Conflict Domain、Residual policy、Recovery Credential和TTL。
- 状态机固定为`draft -> checkpoint_inspected -> compatibility_inspected -> admitted -> submitted`，并允许各非terminal阶段按规则进入`rejected|expired`；submitted只表示exact Plan ref可交Application，不授执行资格。
- 完成create-once、exact historical/current Inspect、CAS revision+1、same-request replay、no-ABA、lost-reply Inspect、内存Repository及SQLite schema v4生产元数据持久化。
- 新增只读`sdk.Client`，覆盖Timeline Inspect/Query/Watch、Checkpoint Manifest/Seal Inspect、RestorePlan history/current Inspect、Retention Inspect及Plan/Credential纯Validate；没有Checkpoint/Restore/Purge/Dispatch直写方法。

## 验证

- `go test ./contract ./domain ./storage/memory ./storage/sqlite ./sdk ./tests/conformance ./tests/fault -run 'RestorePlan' -count=100`：PASS。
- `go test -race ./contract ./domain ./storage/memory ./storage/sqlite ./sdk ./tests/conformance ./tests/fault -run 'RestorePlan' -count=20`：PASS。
- `go test ./... -count=1`：PASS。
- `go test -race ./... -count=1`：PASS。
- `go vet ./...`：PASS。
- `CGO_ENABLED=0 go test ./... -count=1`：PASS。
- `go test -tags continuity_rocksdb ./... -count=1`、`go test -race -tags continuity_rocksdb ./... -count=1`、`go vet -tags continuity_rocksdb ./...`：PASS。
- `gofmt -l .`为空；禁止导入Runtime kernel/foundation/fakes/internal、Harness/Application/Model内部实现的扫描为空。

## 仍未解锁

- RestoreAttempt、Eligibility、Review Authorization、Permit/Fence、Stage、Activate/Abort和任何Provider调用。
- Application namespaced workflow、CLI/API写路径、权限/脱敏production root。
- Checkpoint跨Owner Adapter仍有待用户裁决的exact映射缺口：Runtime中立Seal Ref未携Continuity完整Owner/Scope identity，Runtime typed Participant closure与Continuity Seal通用exact closure尚无唯一映射，Context/Artifact closure digest算法尚未冻结。
