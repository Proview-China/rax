// Package direct bridges the existing request-controlled model-invoker Route
// Gateway into the higher-level execution union. It does not own filesystem,
// process, or tool Effects; those remain Praxis Runtime responsibilities.
package direct
